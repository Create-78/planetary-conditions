---
phase: 06-vercel-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DEPLOYMENT.md
  - README.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
tags: [deployment, vercel, runbook, docs]
must_haves:
  truths:
    - "Repo is verifiably free of committed NASA API keys (git history grep returns 0 real keys; only DEMO_KEY string references survive)"
    - "Build artifact path (dist/) is gitignored and not tracked"
    - "npm run build exits 0 from a clean local environment with the current main commit"
    - "User can follow DEPLOYMENT.md end-to-end and reach a working production deploy without external context"
    - "README.md surfaces a Deployment section linking to DEPLOYMENT.md and shows a Vercel deploy button"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "Step-by-step Vercel deployment runbook"
      contains: "Prerequisites, First-time deployment, Subsequent deploys, Preview deploys, Troubleshooting"
      min_lines: 80
    - path: "README.md"
      provides: "Public-facing project intro + new Deployment section + Vercel deploy button"
      contains: "## Deployment"
  key_links:
    - from: "README.md"
      to: "DEPLOYMENT.md"
      via: "markdown link in Deployment section"
      pattern: "DEPLOYMENT\\.md"
    - from: "README.md"
      to: "https://vercel.com/new/clone"
      via: "Vercel deploy button markdown image link"
      pattern: "vercel\\.com/new/clone"
    - from: "DEPLOYMENT.md"
      to: "https://api.nasa.gov"
      via: "NASA API key registration link in Prerequisites"
      pattern: "api\\.nasa\\.gov"
    - from: "DEPLOYMENT.md"
      to: "VITE_NASA_API_KEY"
      via: "Env var name documented in Vercel Project Settings step"
      pattern: "VITE_NASA_API_KEY"
---

<objective>
Land the **pre-deploy hygiene audit + deployment runbook + README update** as a clean, autonomous diff before Plan 06-02 hands the wheel to the user for the actual Vercel/GitHub steps.

Purpose: Plan 06-02 is the first non-autonomous plan in the entire milestone. Before the user is asked to push to GitHub and set up Vercel, the repo state must be verifiably clean (no committed secrets, gitignore correct, build green) AND the user must have a self-contained `DEPLOYMENT.md` they can follow without context-switching back to this plan. README.md gets a Deployment section + Vercel deploy button so the project is shareable the moment it goes live.

Output:
- New `DEPLOYMENT.md` at repo root (NOT inside `.planning/`) — user-facing runbook with 5 sections
- Updated `README.md` with a `## Deployment` section, Vercel deploy button (placeholder GitHub username `<USER>`), and a "Live demo: (coming soon)" placeholder line
- Hygiene audit results recorded in plan SUMMARY.md (no separate AUDIT file per the Phase 5 inline-audit precedent)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-vercel-deployment/06-CONTEXT.md
@CLAUDE.md
@Discussion.md
@README.md
@.gitignore
@.env.example
@package.json
@src/utils/env.js

<interfaces>
<!-- Extracted from repo state for executor reference. No exploration needed. -->

Current `.gitignore` (verified — already correct, do NOT modify):
```
# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Local env files
.env
.env.local
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
```

Current `src/utils/env.js` (no changes — production behavior identical):
```js
export const NASA_API_KEY = import.meta.env.VITE_NASA_API_KEY || 'DEMO_KEY'
```

Current `package.json` scripts (Vercel auto-detects from this):
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

Current `.env.example` (already shipped Phase 1 — documents the convention):
```
VITE_NASA_API_KEY=DEMO_KEY
```

Vercel deploy button markdown template (verbatim from 06-CONTEXT specifics — use exactly this):
```
[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2F<USER>%2Fplanetary-conditions&env=VITE_NASA_API_KEY&envDescription=NASA%20API%20key%20for%20DONKI%20events&envLink=https%3A%2F%2Fapi.nasa.gov%2F)
```

The literal token `<USER>` stays in the markdown — the user replaces it with their actual GitHub username when they create their repo (DEPLOYMENT.md will instruct them to do so).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy hygiene audit (read-only — surface issues, do not modify code)</name>
  <files>
    <!-- No file modifications in this task. Audit only. Record results in plan SUMMARY at end-of-plan time. -->
  </files>
  <read_first>
    - .gitignore (confirm `.env`, `.env.local`, `dist`, `node_modules` lines present)
    - .env.example (confirm `VITE_NASA_API_KEY=DEMO_KEY` is the only line of substance)
    - package.json (confirm `"build": "vite build"` script exists)
    - src/utils/env.js (confirm `import.meta.env.VITE_NASA_API_KEY || 'DEMO_KEY'`)
    - .planning/phases/06-vercel-deployment/06-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
Run these audit commands from the repo root. Each one MUST be run; capture exact output verbatim for the plan SUMMARY.

**Audit 1 — `.gitignore` covers `.env*` (D-14 item 1):**
```bash
grep -nE '^\.env(\.local)?$' .gitignore
```
Expected: two matching lines (`.env`, `.env.local`).

**Audit 2 — `dist/` is gitignored (D-14 item 3):**
```bash
grep -nE '^dist$' .gitignore
```
Expected: one matching line.

**Audit 3 — No NASA API key committed anywhere in git history (D-14 item 2):**
NASA API keys are 40-char alphanumeric (e.g., `aBcD1234efGh5678ijKl9012mnOp3456qrSt7890`). DEMO_KEY string references are public and acceptable.

```bash
git log --all -p -- ':(exclude)package-lock.json' | grep -iE 'VITE_NASA_API_KEY\s*=\s*[A-Za-z0-9]{30,}' | grep -v 'DEMO_KEY' || echo "AUDIT_PASS: no committed NASA keys"
```
Expected output: literally `AUDIT_PASS: no committed NASA keys` (the `|| echo` fires when grep finds zero matches). If grep finds any lines, audit FAILS — STOP and surface to user.

Also broader check for any 32+ char hex/alphanum tokens adjacent to env-key-shaped strings:
```bash
git log --all -p -- ':(exclude)package-lock.json' | grep -iE '(api[_-]?key|nasa[_-]?key)\s*[:=]\s*["'\'']?[A-Za-z0-9]{20,}' | grep -v 'DEMO_KEY' | grep -v 'VITE_NASA_API_KEY' || echo "AUDIT_PASS: no committed api_key tokens"
```
Expected: `AUDIT_PASS: no committed api_key tokens`. If matches, STOP.

**Audit 4 — `.env` not tracked (D-14 item 1, defense-in-depth):**
```bash
git ls-files | grep -E '^\.env$' && echo "AUDIT_FAIL: .env tracked" || echo "AUDIT_PASS: .env not tracked"
```
Expected: `AUDIT_PASS: .env not tracked`.

**Audit 5 — `dist/` not tracked (D-14 item 3):**
```bash
git ls-files | grep -E '^dist/' && echo "AUDIT_FAIL: dist/ tracked" || echo "AUDIT_PASS: dist/ not tracked"
```
Expected: `AUDIT_PASS: dist/ not tracked`.

**Audit 6 — Build exits 0 (D-14 item 4):**
```bash
npm run build
```
Expected: process exit code 0, terminal output ends with a "✓ built in <time>" line from Vite. Capture: total modules, JS bundle size, CSS bundle size, build duration. The verification grep:
```bash
npm run build 2>&1 | tail -20 | grep -E '✓ built in'
```
Expected: one matching line.

**Audit 7 — `package.json` build script (D-14 item 5):**
```bash
grep -E '"build":\s*"vite build"' package.json
```
Expected: one matching line.

**Audit 8 — Env access discipline (no inline `import.meta.env` outside `src/utils/env.js`, per Phase 4 D-20):**
```bash
grep -rnE 'import\.meta\.env' src/ --include='*.js' --include='*.jsx' | grep -v 'src/utils/env.js'
```
Expected: zero matches (any match indicates env discipline drift — STOP and surface).

**Audit 9 — Only one DEMO_KEY reference (the fallback in env.js):**
```bash
grep -rn 'DEMO_KEY' src/
```
Expected: exactly one match — `src/utils/env.js`.

**Audit 10 — Working tree clean:**
```bash
git status --porcelain
```
Expected: empty output (no uncommitted changes — the plan will introduce DEPLOYMENT.md + README.md changes in Tasks 2 + 3).

**If any audit FAILS:** STOP this plan immediately, do NOT proceed to Task 2, and surface the failure with the exact failing command + its output. Do not attempt remediation in Plan 06-01 — surface to the user for guidance. (Per D-15: "If any pre-deploy check fails, Phase 6 STOPS and surfaces the issue.")

**If all audits PASS:** Record the verbatim command outputs in a local scratch buffer (will be transcribed into the plan SUMMARY at end-of-plan time). Continue to Task 2.
  </action>
  <verify>
    <automated>
      # Aggregate audit re-run as one verification gate.
      set -e
      grep -qE '^\.env$' .gitignore
      grep -qE '^\.env\.local$' .gitignore
      grep -qE '^dist$' .gitignore
      ! git log --all -p -- ':(exclude)package-lock.json' | grep -iE 'VITE_NASA_API_KEY\s*=\s*[A-Za-z0-9]{30,}' | grep -v 'DEMO_KEY' | grep -q .
      ! git ls-files | grep -qE '^\.env$'
      ! git ls-files | grep -qE '^dist/'
      grep -qE '"build":\s*"vite build"' package.json
      ! grep -rnE 'import\.meta\.env' src/ --include='*.js' --include='*.jsx' | grep -v 'src/utils/env.js' | grep -q .
      [ "$(grep -rl 'DEMO_KEY' src/ | wc -l | tr -d ' ')" = "1" ]
      [ -z "$(git status --porcelain)" ]
      npm run build > /tmp/06-01-build.log 2>&1
      grep -qE '✓ built in' /tmp/06-01-build.log
    </automated>
  </verify>
  <acceptance_criteria>
    - All 10 audits return PASS (verify script exits 0)
    - `npm run build` completes with non-empty `dist/` directory
    - Verbatim audit output is captured for inclusion in plan SUMMARY
    - No files modified in this task (git status remains clean post-audit)
  </acceptance_criteria>
  <done>
    Hygiene audit complete; all 10 checks pass; build green; ready to author runbook in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DEPLOYMENT.md runbook at repo root</name>
  <files>DEPLOYMENT.md</files>
  <read_first>
    - .planning/phases/06-vercel-deployment/06-CONTEXT.md (D-04, D-05, D-08, D-10, D-11, D-12, D-13, D-16, D-17 — all the runbook content decisions)
    - Discussion.md §3 (env var rationale), §8 (deployment checklist), §9 (API key registration)
    - .env.example (one-line `VITE_NASA_API_KEY=DEMO_KEY` — referenced from runbook)
    - README.md (so the runbook's tone matches the existing README voice)
  </read_first>
  <action>
Create `/Users/mc/Library/CloudStorage/GoogleDrive-will.mcdermott@wecreate78.com/My Drive/_Personal/planetary-conditions/DEPLOYMENT.md` (i.e., at the **repo root**, NOT inside `.planning/`) with EXACTLY the content below. Per D-16/D-17, this is a user-facing runbook — the user reads it without GSD context.

Use this exact content (the literal string `<USER>` is preserved — users replace it with their GitHub username when they fork/clone):

```markdown
# Deployment

This runbook walks you through deploying **Planetary Conditions** to Vercel from scratch. Time: ~10 minutes once you have GitHub and Vercel accounts.

> The app is a static SPA — no backend, no Serverless Functions. Vercel auto-detects Vite and builds the project with zero configuration.

---

## Prerequisites

Before you start, you need:

1. **Node.js** (v18 or later) installed locally — for the pre-deploy build check.
2. **A GitHub account** — to host the repo.
3. **A Vercel account** — free Hobby tier is sufficient. Sign up at [vercel.com](https://vercel.com) and link your GitHub account during signup (Vercel will use it to import the project).
4. **(Recommended) A NASA API key** — free, 1000 requests/hour. Register at [api.nasa.gov](https://api.nasa.gov) — the form takes 30 seconds and the key arrives by email instantly.
   - Without your own key, the app falls back to `DEMO_KEY` (30 requests/hour) which is fine for local dev but will rate-limit quickly in production.

---

## First-time deployment

### 1. Confirm the project builds locally

From the project root:

```bash
npm install
npm run build
```

You should see Vite output ending with `✓ built in <time>`. If the build fails, fix the failure before continuing — Vercel will hit the same error.

### 2. Push the repo to GitHub

If you haven't already:

```bash
# Create a new repository on github.com first (any visibility — public recommended)
# Then, from the project root:
git remote add origin https://github.com/<USER>/planetary-conditions.git
git branch -M main
git push -u origin main
```

Replace `<USER>` with your GitHub username.

> Verify `.env` is NOT in your push — it should be gitignored. Run `git ls-files | grep '\.env$'` and confirm zero output before pushing.

### 3. Import the project in Vercel

Two equivalent paths — pick one:

**Path A — Vercel dashboard (recommended for first-time users):**
1. Go to [vercel.com/new](https://vercel.com/new)
2. Click **Add New… → Project**
3. Select your `planetary-conditions` repo from the GitHub list
4. On the configuration screen, **leave all defaults**:
   - Framework Preset: **Vite** (auto-detected)
   - Build Command: `npm run build` (auto-detected)
   - Output Directory: `dist` (auto-detected)
   - Install Command: `npm install` (auto-detected)
5. **Before clicking Deploy**, expand **Environment Variables** and add the env var from the next step.

**Path B — Vercel CLI:**
```bash
npm install -g vercel
vercel login
vercel link
# Accept the defaults (link to existing project: No; name: planetary-conditions; directory: ./)
```

### 4. Set the NASA API key in Vercel

In the Vercel dashboard, go to **Project → Settings → Environment Variables** and add:

| Field | Value |
|-------|-------|
| **Name** | `VITE_NASA_API_KEY` |
| **Value** | Your NASA API key from api.nasa.gov (or `DEMO_KEY` if you skipped registration) |
| **Environments** | ☑ Production, ☑ Preview (leave Development unchecked — local `.env` handles dev) |

Click **Save**.

> **Why both Production and Preview?** PR preview deploys need to hit NASA DONKI too. If only Production is checked, previews will fall back to `DEMO_KEY` and may rate-limit during review.

### 5. Trigger the first deploy

- **Path A users:** Click **Deploy** on the import screen. Vercel runs the first build (~1-2 minutes).
- **Path B users:** Push to `main` (`git push`) or run `vercel --prod` once. Either triggers a deploy via Vercel's GitHub integration.

Watch the build logs in the Vercel dashboard. Expected: `Build Completed in <project>/dist`. Vercel shows the production URL when the deploy finishes — typically `https://planetary-conditions.vercel.app` (or `https://planetary-conditions-<random>.vercel.app` if the name was taken).

### 6. Verify the production URL

Open the production URL in a browser, then:

1. Open DevTools → **Network** tab → filter to `Fetch/XHR`.
2. Switch to the **Mars** tab. Confirm 1 successful GET to `https://api.maas2.apollorion.com/` (status 200).
3. Switch to the **Moon** tab. Confirm 6 successful GETs:
   - 3 to `services.swpc.noaa.gov` (`plasma-2-hour.json`, `mag-2-hour.json`, `noaa-planetary-k-index.json`)
   - 3 to `api.nasa.gov/DONKI/` (`CME`, `FLR`, `GST`)
4. Open DevTools → **Console**. Confirm **zero** `CORS policy: ...blocked...` errors.
5. Inspect the DONKI request URLs — confirm `api_key=<your-key>` (NOT `api_key=DEMO_KEY`) in the query string. If you see `DEMO_KEY`, the env var didn't propagate — re-check step 4 then redeploy from the Vercel dashboard (Deployments → ⋯ → Redeploy).

### 7. Smoke test both tabs

On the production URL:

- **Mars tab:** 8 DataCards render with real values (sol number, Earth date, min/max temp, pressure, wind speed, humidity, opacity) — no em-dashes everywhere, no "Data temporarily unavailable" banners.
- **Moon tab — Lunar Context:** Current lunar phase name, percentage, day/night indicator, estimated surface temperature.
- **Moon tab — Space Weather:** Solar wind speed, density, Bz, Kp index, and a colored Radiation Risk badge (Low/Moderate/High).
- **Moon tab — Solar Event Alerts:** Either a scrollable list of recent CME/FLR/GST events OR the empty-state copy "No significant events in the past 7 days — conditions are calm."

If all four sections render real data with no console errors, the deploy is verified.

---

## Subsequent deploys

Every push to `main` triggers an auto-deploy:

```bash
git push origin main
```

Watch the Vercel dashboard (**Deployments** tab) for build status. Most builds complete in under 60 seconds.

---

## Preview deploys

Every push to a non-`main` branch (and every pull request) generates a unique preview URL automatically. Useful for:

- Reviewing visual changes before merging
- Testing with the Preview-scope env var
- Sharing in-progress work without affecting production

Preview URLs are posted as a comment on the PR by Vercel's GitHub app.

---

## Troubleshooting

### Build fails on Vercel but works locally
- Confirm `node_modules` is in `.gitignore` and was NOT committed.
- Confirm `package-lock.json` IS committed — Vercel uses it for reproducible installs.
- Check Vercel's Node version (Settings → General → Node.js Version) — set to 18.x or newer.

### CORS errors in production console
All three upstream APIs (MAAS2, NOAA SWPC, NASA DONKI) are documented CORS-open and have been verified from production. If you see CORS errors:
1. Confirm the request URL exactly matches the documented endpoint (typos break CORS preflight).
2. Check that you're not behind a corporate proxy that strips CORS headers.
3. If a specific upstream genuinely starts blocking the deployed origin, the contingency is a Vercel Edge Function proxy — but this is **not** part of v1 unless evidence shows the rate limit or CORS block is unavoidable. See `CLAUDE.md` for the proxy decision rule.

### NASA DONKI returns 403 / "OVER_RATE_LIMIT"
You're hitting `DEMO_KEY`'s 30/hr cap. Register a real key at [api.nasa.gov](https://api.nasa.gov) and update `VITE_NASA_API_KEY` in Vercel (Settings → Environment Variables → edit → Save). Redeploy: **Deployments → latest → ⋯ → Redeploy**.

### "VITE_NASA_API_KEY is undefined" at runtime
- Confirm the env var name is **exact**: `VITE_NASA_API_KEY` (the `VITE_` prefix is required — Vite only inlines vars with that prefix into the client bundle at build time).
- Confirm the var is set on **Production** scope (and Preview if you want preview deploys to use the real key).
- After adding/editing an env var, you MUST redeploy — Vercel does NOT hot-swap env vars into existing builds.

### Production URL shows a 404 or blank screen
- Check Vercel's build log for errors.
- Confirm `dist/index.html` was generated locally (`npm run build` then `ls dist/`).
- Confirm the Output Directory in Vercel settings is `dist` (not `build` or `out`).

---

*This runbook is the deployment source-of-truth. If you change deployment infrastructure (custom domain, env vars, build settings), update this file in the same commit.*
```

Save the file. Confirm with `wc -l DEPLOYMENT.md` — should be 130+ lines.
  </action>
  <verify>
    <automated>
      set -e
      [ -f DEPLOYMENT.md ]
      # Must be at repo root, not nested
      [ ! -f .planning/DEPLOYMENT.md ]
      [ ! -f .planning/phases/06-vercel-deployment/DEPLOYMENT.md ]
      # Required section headings (D-16)
      grep -q '^## Prerequisites' DEPLOYMENT.md
      grep -q '^## First-time deployment' DEPLOYMENT.md
      grep -q '^## Subsequent deploys' DEPLOYMENT.md
      grep -q '^## Preview deploys' DEPLOYMENT.md
      grep -q '^## Troubleshooting' DEPLOYMENT.md
      # Key references
      grep -q 'api\.nasa\.gov' DEPLOYMENT.md
      grep -q 'VITE_NASA_API_KEY' DEPLOYMENT.md
      grep -q 'DEMO_KEY' DEPLOYMENT.md
      grep -q 'vercel\.com' DEPLOYMENT.md
      grep -q 'Framework Preset.*Vite' DEPLOYMENT.md
      grep -q 'dist' DEPLOYMENT.md
      grep -q 'main' DEPLOYMENT.md
      # 6 upstream fetches enumerated in the verification step
      grep -q 'maas2\.apollorion\.com' DEPLOYMENT.md
      grep -q 'services\.swpc\.noaa\.gov' DEPLOYMENT.md
      grep -q 'DONKI' DEPLOYMENT.md
      # The <USER> placeholder is intentional (user replaces when they create their repo)
      grep -q '<USER>' DEPLOYMENT.md
      # File is substantive
      [ "$(wc -l < DEPLOYMENT.md | tr -d ' ')" -ge 80 ]
      # MUST NOT instruct Claude or user-facing prose to run `vercel deploy --prod` in the normal flow (D-02)
      # vercel --prod is mentioned ONLY in Path B Step 5; verify it does not appear elsewhere as the recommended path
      [ "$(grep -c 'vercel --prod' DEPLOYMENT.md | tr -d ' ')" -le 1 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `DEPLOYMENT.md` exists at repo root (not inside `.planning/`)
    - File contains all 5 required section headings: Prerequisites, First-time deployment, Subsequent deploys, Preview deploys, Troubleshooting (D-16)
    - File mentions: `VITE_NASA_API_KEY`, `api.nasa.gov`, `DEMO_KEY`, `Vite` framework preset, `dist` output dir, `main` branch
    - File enumerates the 7 upstream fetches the user will verify in step 6 (1 MAAS2 + 3 SWPC + 3 DONKI)
    - `<USER>` placeholder string is present (per intentional user-replaces-on-fork pattern)
    - File is at least 80 lines long
    - `vercel --prod` mentioned at most once (only as Path B optional alternative, per D-02)
  </acceptance_criteria>
  <done>
    DEPLOYMENT.md committed-ready at repo root; runbook is self-sufficient for a user with no GSD context.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with Deployment section + Vercel deploy button</name>
  <files>README.md</files>
  <read_first>
    - README.md (the existing file — preserve its structure and tone)
    - DEPLOYMENT.md (just authored — README links to it)
    - .planning/phases/06-vercel-deployment/06-CONTEXT.md (D-18, D-19 — placeholder "Live demo (coming soon)" until URL known; deploy button specifics)
  </read_first>
  <action>
Modify `README.md` to add:

1. **A "Live demo" placeholder line near the top** — per D-19, use `(coming soon)` text until the user confirms the production URL in Plan 06-02. Insert this immediately after the existing tagline block (after the `Why not "weather"?` paragraph), as a new section block before `---` / Status.

2. **A new `## Deployment` section** — placed after the existing `## Tech Stack` section and before `## Quick Links`. Section contents:
   - One-paragraph summary (2-3 sentences) that the app deploys to Vercel via GitHub auto-deploy, with `VITE_NASA_API_KEY` configured in the Vercel dashboard
   - The Vercel deploy button (from the canonical markdown template in 06-CONTEXT specifics — paste **literally**, including the `<USER>` placeholder)
   - A link sentence: "Full step-by-step instructions: see [DEPLOYMENT.md](DEPLOYMENT.md)."

Use the Edit tool (not Write — preserve untouched content). Apply these specific edits:

**Edit A — Insert "Live demo" placeholder after the tagline paragraph (after line 7 in current README.md):**

After the line:
```
Why not "weather"? The Moon has no atmosphere. "Planetary Conditions" is more precise — and more interesting.
```

Insert a blank line, then:
```
**Live demo:** (coming soon — will be updated when first Vercel deploy completes)
```

**Edit B — Insert new `## Deployment` section before `## Quick Links` (currently around line 39):**

Find the existing line `## Quick Links` and insert the following block BEFORE it (followed by one blank line, then `## Quick Links` unchanged):

```markdown
## Deployment

The app deploys to Vercel as a static SPA with auto-deploy from `main`. Vercel auto-detects the Vite framework — no `vercel.json` config needed. Set `VITE_NASA_API_KEY` in the Vercel dashboard (Project → Settings → Environment Variables, Production + Preview scopes).

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2F<USER>%2Fplanetary-conditions&env=VITE_NASA_API_KEY&envDescription=NASA%20API%20key%20for%20DONKI%20events&envLink=https%3A%2F%2Fapi.nasa.gov%2F)

Full step-by-step instructions: see [DEPLOYMENT.md](DEPLOYMENT.md).

---

```

**Do not** change any other lines, do not reorder existing sections, do not modify the Status, Current Priorities, Tech Stack, Quick Links, Team, Recent Updates, or Getting Started sections beyond inserting the two blocks above.

**Do not** add today's date to Recent Updates yet — that update happens in Plan 06-02 Task 7 after the user confirms the live URL.
  </action>
  <verify>
    <automated>
      set -e
      [ -f README.md ]
      grep -q '## Deployment' README.md
      grep -q '\*\*Live demo:\*\* (coming soon' README.md
      grep -q 'DEPLOYMENT\.md' README.md
      grep -q 'vercel\.com/button' README.md
      grep -q 'vercel\.com/new/clone' README.md
      grep -q 'github\.com%2F<USER>' README.md
      grep -q 'VITE_NASA_API_KEY' README.md
      # Preserve existing structure
      grep -q '^# Planetary Conditions' README.md
      grep -q '^## Status' README.md
      grep -q '^## Current Priorities' README.md
      grep -q '^## Tech Stack' README.md
      grep -q '^## Quick Links' README.md
      grep -q '^## Team' README.md
      grep -q '^## Getting Started' README.md
      # Deployment section appears before Quick Links
      [ "$(grep -n '^## Deployment' README.md | cut -d: -f1)" -lt "$(grep -n '^## Quick Links' README.md | cut -d: -f1)" ]
      # Live demo line appears before Status
      [ "$(grep -n '\*\*Live demo:\*\*' README.md | cut -d: -f1)" -lt "$(grep -n '^## Status' README.md | cut -d: -f1)" ]
      # No actual production URL committed (D-19: only placeholder until user confirms)
      ! grep -qE 'https://planetary-conditions[a-z0-9-]*\.vercel\.app' README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a `## Deployment` section between `## Tech Stack` and `## Quick Links`
    - Deployment section contains Vercel deploy button markdown with `<USER>` placeholder and `VITE_NASA_API_KEY` env params
    - Deployment section links to `DEPLOYMENT.md` (relative link)
    - "**Live demo:** (coming soon ...)" line appears in the top portion of README (before `## Status`), per D-19
    - No actual `*.vercel.app` URL is present yet (placeholder text only — real URL added in Plan 06-02 Task 7)
    - All pre-existing README sections preserved unmodified (Status, Current Priorities, Tech Stack, Quick Links, Team, Recent Updates, Getting Started)
  </acceptance_criteria>
  <done>
    README.md surfaces the deploy story (button + link to runbook) with placeholder URL pending user-confirmed production deploy in 06-02.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → GitHub (push) | Pushing code to a remote crosses a trust boundary — any committed secret becomes effectively public (even private repos leak history if visibility flips) |
| Repo → Vercel build | Vercel reads the repo, runs `npm install` + `npm run build`, embeds `VITE_*` env vars into the client bundle at build time |
| Vercel-hosted bundle → end users | The built bundle is served to anyone with the production URL; any env var inlined into JS is effectively public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I (Information disclosure) | git history | mitigate | Task 1 audits `git log -p` for any committed NASA API key strings; STOP plan if any 30+ char alphanumeric token paired with `VITE_NASA_API_KEY` or `api_key` is found. Only `DEMO_KEY` references are acceptable (public string). |
| T-06-02 | I | `.env` file | mitigate | Task 1 audits `.gitignore` for `.env` + `.env.local` lines AND verifies `git ls-files | grep '\.env$'` returns nothing. Phase 1 Plan 01 already shipped this; Task 1 re-verifies as a regression check. |
| T-06-03 | I | `dist/` build artifact | mitigate | Task 1 audits `.gitignore` excludes `dist` AND `git ls-files | grep '^dist/'` returns nothing. Static SPA bundles can contain inlined env-var values; not committing them is the simplest seal. |
| T-06-04 | I | `VITE_NASA_API_KEY` exposure to clients | accept | Vite inlines `VITE_*` vars at build time — they appear in the served JS bundle in plaintext. **Accepted risk** per CLAUDE.md and Discussion.md §3: NASA API keys are low-sensitivity (free, rate-limited, no PII access). DEPLOYMENT.md documents this convention explicitly. If higher-sensitivity keys are added later, route through a server-side proxy. |
| T-06-05 | T (Tampering) | DEPLOYMENT.md instructions | accept | DEPLOYMENT.md is user-facing prose; tampering risk is limited to confusing the user. Mitigation: file lives at repo root (high visibility), is committed via plan SUMMARY-tracked workflow, and is referenced from README.md. |
| T-06-06 | D (Denial of service) | NASA DONKI rate limit | mitigate | D-05 mandates registering a real NASA key (1000/hr) instead of shipping `DEMO_KEY` (30/hr) to production. DEPLOYMENT.md Prerequisites step 4 and step 4-Set-the-NASA-API-key make this the recommended path; the Troubleshooting section documents the symptom and fix. |
| T-06-07 | E (Elevation of privilege) | Vercel/GitHub OAuth flow | accept | Vercel ↔ GitHub integration uses Vercel's GitHub App with scoped permissions. Authentication flow happens entirely in user's browser; Claude never handles tokens. No credential storage in repo. |

</threat_model>

<verification>

**Plan-level checks (run after all 3 tasks complete):**

1. `DEPLOYMENT.md` exists at repo root and is at least 80 lines long
2. README.md `## Deployment` section exists and links to DEPLOYMENT.md
3. README.md has placeholder `**Live demo:** (coming soon` text — NO actual vercel.app URL yet
4. `git status` shows exactly two modified files: `DEPLOYMENT.md` (new) and `README.md` (modified)
5. `npm run build` still exits 0 (no regression from doc changes — defensive re-check)
6. All 10 Task 1 audits still pass (no regressions introduced)

**Aggregate verification command:**
```bash
set -e
[ -f DEPLOYMENT.md ]
[ "$(wc -l < DEPLOYMENT.md | tr -d ' ')" -ge 80 ]
grep -q '^## Deployment' README.md
grep -q 'DEPLOYMENT\.md' README.md
grep -q '\*\*Live demo:\*\* (coming soon' README.md
! grep -qE 'https://planetary-conditions[a-z0-9-]*\.vercel\.app' README.md
npm run build > /tmp/06-01-final-build.log 2>&1
grep -qE '✓ built in' /tmp/06-01-final-build.log
```

</verification>

<success_criteria>

Plan 06-01 succeeds when:

- [ ] All 10 hygiene audits in Task 1 PASS (no committed secrets, `.env`/`.dist` properly gitignored, build green, env discipline intact)
- [ ] `DEPLOYMENT.md` exists at repo root with the 5 required sections (Prerequisites, First-time deployment, Subsequent deploys, Preview deploys, Troubleshooting) and is self-sufficient for a user with no GSD context
- [ ] `README.md` has a `## Deployment` section with the Vercel deploy button + link to DEPLOYMENT.md
- [ ] `README.md` has a placeholder `**Live demo:** (coming soon ...)` line — NOT a real URL (real URL is set in Plan 06-02 Task 7)
- [ ] `npm run build` still exits 0 post-changes
- [ ] No source code (`src/**`) modified by this plan — docs and runbooks only
- [ ] No git remote added, no push attempted, no Vercel CLI invoked — those are all Plan 06-02 user actions

</success_criteria>

<output>

After completion, create `.planning/phases/06-vercel-deployment/06-01-SUMMARY.md` capturing:

- Verbatim output of the 10 Task 1 hygiene audits (the AUDIT_PASS lines and the build size/duration line)
- Confirmation that `DEPLOYMENT.md` lives at repo root with all 5 sections present
- Confirmation README.md got the Deployment section + Vercel button + (coming soon) placeholder
- Two commit SHAs (one for DEPLOYMENT.md, one for README.md changes) — or one combined commit, planner's discretion
- Carryforward note: "Plan 06-02 picks up with user-action gates. Repo is hygiene-clean and ready for the first push to GitHub."

</output>
