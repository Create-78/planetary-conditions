---
phase: 8.5
slug: full-background-glass
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 8.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node built-in test runner (`node:test`) |
| **Config file** | none — invoked via `npm test` → `node --test "src/**/*.test.js"` |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm test && npm run build` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm test && npm run build`
- **Before `/gsd-verify-work`:** Full suite must be green + human visual verify
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 8.5-01-01 | 01 | 1 | LAYOUT-01 | — | N/A | manual | n/a — CSS layout requires browser | ✅ plan | ⬜ pending |
| 8.5-01-02 | 01 | 1 | LAYOUT-02 | — | N/A | manual | n/a — backdrop-filter requires browser | ✅ plan | ⬜ pending |
| 8.5-01-03 | 01 | 1 | LAYOUT-03 | — | N/A | unit + manual | `npm test` (existing Phase 8 hook tests) | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — existing test infrastructure from Phase 8 covers all automated requirements. The `useScrollParallax.test.js` and `usePrefersReducedMotion.test.js` from Phase 8 remain the correctness gate for LAYOUT-03.

*Existing infrastructure covers all automated phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Full-bleed hero fills viewport, no 16:7 box | LAYOUT-01 | CSS layout — visual verification required | Open both tabs, confirm hero spans full width with no containing aspect-ratio box |
| Glassmorphic panel legible over both hero images | LAYOUT-02 | `backdrop-filter` requires running browser | Hover/focus data cards — text must be clearly readable over the Mars and Moon images |
| Cross-fade smooth over full-background image | LAYOUT-03 | Visual animation quality | Switch tabs — backdrop and hero should dissolve, not snap |
| Parallax visible (~10px) with full-background image | LAYOUT-03 | Perceptual quality now more prominent on full-bg | Scroll slowly — hero should drift noticeably (more perceptible than Phase 8 contained box) |
| Reduced-motion: instant tab switch + static hero | LAYOUT-03 | Requires OS setting change | Enable macOS "Reduce Motion" — tab switch instant, scroll static |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
