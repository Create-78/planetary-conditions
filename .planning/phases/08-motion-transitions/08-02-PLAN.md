---
phase: 08-motion-transitions
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/App.jsx
autonomous: false
requirements: [MOTION-01, MOTION-02, MOTION-03]

must_haves:
  truths:
    - "App.jsx calls usePrefersReducedMotion() once and stores the result as reducedMotion"
    - "App.jsx passes reducedMotion to both AtmosphericBackdrop and BodyHero alongside activeTab"
    - "Switching tabs cross-fades backdrop and hero instead of snapping"
    - "Scrolling drifts the hero ~10px (gentle parallax), not the data panel below it"
    - "With prefers-reduced-motion: reduce, tab switches are instant and the hero is static"
  artifacts:
    - path: "src/App.jsx"
      provides: "Reduced-motion signal threaded to both motion components"
      contains: "usePrefersReducedMotion"
  key_links:
    - from: "src/App.jsx"
      to: "src/hooks/usePrefersReducedMotion.js"
      via: "import + single hook call"
      pattern: "usePrefersReducedMotion"
    - from: "src/App.jsx"
      to: "AtmosphericBackdrop / BodyHero"
      via: "reducedMotion prop pass"
      pattern: "reducedMotion={reducedMotion}"
---

<objective>
Thread the reduced-motion signal through `App.jsx` to both motion components, then verify the full phase end-to-end (build + test suite + live human visual check of cross-fade, parallax, and the reduced-motion path).

Purpose: This is the integration wave. Plan 01 built the hooks and refactored the components in isolation; this plan wires `usePrefersReducedMotion()` into the live tree so all three requirements (MOTION-01/02/03) become observable, then gates completion on the human-verify checkpoint mandated by VALIDATION.md.
Output: Updated `App.jsx`; green build + test suite; approved visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-motion-transitions/08-UI-SPEC.md
@.planning/phases/08-motion-transitions/08-VALIDATION.md

<interfaces>
<!-- usePrefersReducedMotion contract (created in Plan 08-01): -->
import { usePrefersReducedMotion } from './hooks/usePrefersReducedMotion.js'
// Returns boolean. Initial false (animate), updates to OS value after mount; tracks 'change' events.

<!-- Current App.jsx render shape (additive change only — preserve everything else): -->
const [activeTab, setActiveTab] = useState(DEFAULT_TAB)
<AtmosphericBackdrop activeTab={activeTab} />
...
<BodyHero activeTab={activeTab} />

<!-- Both components now accept ({ activeTab, reducedMotion }) after Plan 08-01. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread reducedMotion through App.jsx</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (CURRENT — confirm activeTab state and the two component call sites)
    - src/hooks/usePrefersReducedMotion.js (created in Plan 08-01 — confirm import path/export name)
    - .planning/phases/08-motion-transitions/08-RESEARCH.md §"Threading reducedMotion in App.jsx"
  </read_first>
  <action>
    Make ONLY additive changes to App.jsx. Do not touch StarField, the header, PlanetaryIcon, TabBar wiring, the data-panel wrapper, or the MarsTab/MoonTab conditional render.

    1. Add the import below the existing `BodyHero` import:
       ```jsx
       import { usePrefersReducedMotion } from './hooks/usePrefersReducedMotion.js'
       ```
    2. Inside `App()`, immediately after `const [activeTab, setActiveTab] = useState(DEFAULT_TAB)`, add:
       ```jsx
       const reducedMotion = usePrefersReducedMotion()
       ```
    3. Update the two component call sites to pass the prop:
       ```jsx
       <AtmosphericBackdrop activeTab={activeTab} reducedMotion={reducedMotion} />
       ```
       ```jsx
       <BodyHero activeTab={activeTab} reducedMotion={reducedMotion} />
       ```
    No other lines change.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -q "import { usePrefersReducedMotion } from './hooks/usePrefersReducedMotion.js'" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -q "import { usePrefersReducedMotion } from './hooks/usePrefersReducedMotion.js'" src/App.jsx` succeeds
    - `grep -q "const reducedMotion = usePrefersReducedMotion()" src/App.jsx` succeeds
    - `grep -q "<AtmosphericBackdrop activeTab={activeTab} reducedMotion={reducedMotion} />" src/App.jsx` succeeds
    - `grep -q "<BodyHero activeTab={activeTab} reducedMotion={reducedMotion} />" src/App.jsx` succeeds
    - `grep -c 'reducedMotion={reducedMotion}' src/App.jsx` returns 2 (threaded to exactly both components)
    - `grep -q 'StarField' src/App.jsx` still succeeds (existing render untouched)
  </acceptance_criteria>
  <done>App.jsx imports and calls usePrefersReducedMotion once and passes reducedMotion to both AtmosphericBackdrop and BodyHero; build passes; all prior render structure intact.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite verification of the integrated phase</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/08-motion-transitions/08-VALIDATION.md §"Per-Task Verification Map" and §"Full suite command"
  </read_first>
  <action>
    Run the full Phase 8 verification suite and confirm every gate is green before the human checkpoint. Run, in order:
    1. `npm run build` — must exit 0.
    2. `npm test` — must report `fail 0`.
    3. The VALIDATION.md full-suite grep: `grep -r 'reducedMotion\|prefers-reduced-motion\|useScrollParallax' src/` — must return matches across App.jsx, both hooks, AtmosphericBackdrop.jsx, and BodyHero.jsx.
    4. The zero-new-deps gate: `git diff --stat package.json package-lock.json` — must be empty.
    5. The no-motion-library gate: `grep -rE 'framer-motion|gsap|react-scroll-parallax' src/ package.json` — must return nothing.

    If any gate fails, fix the offending file (within its owning plan's scope) and re-run before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm run build && npm test 2>&1 | grep -q 'fail 0' && grep -rq 'reducedMotion' src/ && ! grep -rqE 'framer-motion|gsap|react-scroll-parallax' src/ package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` output contains `fail 0`
    - `grep -rl 'reducedMotion' src/` lists at least: src/App.jsx, src/components/AtmosphericBackdrop.jsx, src/components/BodyHero.jsx
    - `grep -rl 'useScrollParallax' src/` lists src/hooks/useScrollParallax.js and src/components/BodyHero.jsx
    - `grep -rl 'prefers-reduced-motion' src/` lists src/hooks/usePrefersReducedMotion.js
    - `grep -rE 'framer-motion|gsap|react-scroll-parallax' src/ package.json` returns nothing (exit 1)
    - `git diff --stat package.json` is empty
  </acceptance_criteria>
  <done>Build is green, the node:test suite reports fail 0, all motion identifiers are wired across the expected files, and no motion library was introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of cross-fade, parallax, and reduced-motion</name>
  <what-built>
    Phase 8 motion is fully wired: AtmosphericBackdrop and BodyHero cross-fade on tab switch (MOTION-01), the hero drifts ~10px on scroll (MOTION-02), and a reduced-motion OS setting disables both (MOTION-03). All automated gates (build, node:test, no-new-deps, no-motion-library) are green.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the local URL (typically http://localhost:5173).

    1. **Cross-fade (MOTION-01):** Click between the Mars and Moon tabs. Expected: both the full-bleed backdrop AND the framed hero dissolve from one body to the other over ~500ms — a smooth cross-fade, NOT an instant pop or a blank flash. Watch the hero specifically: the new body's LQIP/image should fade in as the old fades out, no white/blank frame in between.
    2. **Cross-fade duration feel (MOTION-01):** The transition should feel snappy but deliberate — not instant, not sluggish. The locked value is 500ms; confirm it reads right against the blurred backdrop.
    3. **Parallax (MOTION-02):** Scroll the page down slowly. Expected: the hero image drifts gently (~10px at ~200px of scroll) — reads as subtle depth, not jitter or jumpiness. The data panel BELOW the hero must NOT shift position (if it does, the ref is on the wrong element). On the first 1px of scroll the hero must NOT jump to a wrong crop (that would mean the -50% center-crop was clobbered).
    4. **Reduced-motion (MOTION-03):** Enable macOS System Settings → Accessibility → Display → "Reduce motion" (toggle ON). Return to the app — no refresh needed (the hook listens for `change`). Now: switching tabs should be INSTANT (no fade), and scrolling should leave the hero perfectly STATIC (no parallax drift). Toggle Reduce motion back OFF and confirm the animations return.
  </how-to-verify>
  <resume-signal>Type "approved" if all four behaviors are correct, or describe any issue (e.g. "hero jumps on first scroll", "tab switch still pops", "reduced-motion still animates", "duration feels too slow") so it can be fixed before the phase closes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wiring-only plan; no untrusted-input boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T (Tampering) | App.jsx prop threading | accept | reducedMotion is a derived boolean from a read-only browser API; no client-tamperable security decision rides on it |

Security domain NOT APPLICABLE for this phase (RESEARCH.md §"Security Domain"). This plan only passes a boolean prop.
</threat_model>

<verification>
- `npm run build` exits 0
- `npm test` reports `fail 0`
- `reducedMotion={reducedMotion}` appears exactly twice in App.jsx
- Full-suite grep finds motion identifiers across App.jsx, both hooks, and both components
- `git diff package.json` empty; no motion library present
- Human-verify checkpoint approved (cross-fade smooth, parallax ~10px no panel shift, reduced-motion disables both)
</verification>

<success_criteria>
- App.jsx threads reducedMotion to both AtmosphericBackdrop and BodyHero
- All automated gates green (build, test, no-new-deps, no-motion-library)
- MOTION-01, MOTION-02, MOTION-03 visually confirmed by human checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-motion-transitions/08-02-SUMMARY.md`
</output>
