---
phase: 07-hero-atmospheric-backdrop
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/App.jsx
  - src/components/StarField.jsx
autonomous: false
requirements: [HERO-03, HERO-04, IMG-03]

must_haves:
  truths:
    - "Opening either tab shows the matching body as both a full-bleed backdrop and a framed hero between the toggle and the data panel"
    - "Switching Mars ↔ Moon swaps both the backdrop and the hero to the matching body"
    - "Data panel and card text stay high-contrast and fully legible over the backdrop — no contrast regression vs v1.0"
    - "The hero shows an instant LQIP blur-up with no layout shift, and per-tab added image weight stays within the ~300 KB WebP budget"
  artifacts:
    - path: "src/App.jsx"
      provides: "Mounted AtmosphericBackdrop + BodyHero in correct z-order, glass data panel"
      contains: "AtmosphericBackdrop activeTab={activeTab}"
    - path: "src/components/StarField.jsx"
      provides: "StarField opacity reduced so backdrop reads through"
      contains: "opacity: 0.4"
  key_links:
    - from: "src/App.jsx"
      to: "AtmosphericBackdrop + BodyHero (Plan 02 components)"
      via: "import + mount, AtmosphericBackdrop before StarField, BodyHero between TabBar and data panel"
      pattern: "<AtmosphericBackdrop activeTab=\\{activeTab\\}"
    - from: "src/App.jsx activeTab state"
      to: "both new components"
      via: "activeTab prop threaded to backdrop + hero so the swap is synchronized"
      pattern: "<BodyHero activeTab=\\{activeTab\\}"
    - from: "data panel div"
      to: "glassmorphism over backdrop"
      via: "bg-slate-950/30 backdrop-blur-md (subject to contrast guardrail)"
      pattern: "bg-slate-950/(30|40|60)"
---

<objective>
Wire the Phase 7 layered frame together in App.jsx, reduce StarField opacity so the backdrop reads through, push the data panel slightly more translucent (glassmorphism), and then run the non-negotiable live verification: WCAG AA contrast (HERO-04), CLS ≈ 0 (IMG-02), and the ~300 KB per-tab WebP budget (IMG-03). A human-verify checkpoint confirms both tabs show the correct backdrop + hero, the swap works (HERO-03), and data legibility has not regressed vs v1.0.

Purpose: This is the integration + acceptance plan. It satisfies HERO-03 (synchronized Mars↔Moon swap of both layers), HERO-04 (no contrast regression — the D-06 guardrail that overrides the target look), and IMG-03 (measured per-tab weight within budget). The contrast guardrail (D-06) is enforced here: if data text fails AA, the executor dials the tuning levers (backdrop brightness down, panel opacity up) before the checkpoint.

Output: A fully integrated layered dashboard, validated against contrast/CLS/perf, with a human sign-off on visual correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-hero-atmospheric-backdrop/07-CONTEXT.md
@.planning/phases/07-hero-atmospheric-backdrop/07-UI-SPEC.md
@.planning/phases/07-hero-atmospheric-backdrop/07-PATTERNS.md
@docs/superpowers/specs/2026-05-28-v2-visual-upgrade-design.md

<interfaces>
<!-- The exact current App.jsx and StarField.jsx state the executor edits. Extracted from codebase. -->

Current src/App.jsx return (the surgical insertion target):
```jsx
<div className="relative min-h-screen w-full font-sans text-slate-100 antialiased">
  <StarField />

  <main className="relative z-10 mx-auto max-w-6xl px-4 sm:px-6 md:px-8 py-8 md:py-12">
    <header className="mb-8 md:mb-12">...</header>

    <div className="mb-8 md:mb-10">
      <TabBar activeTab={activeTab} onTabChange={setActiveTab} />
    </div>

    <div className="rounded-lg border border-slate-800/60 bg-slate-950/40 backdrop-blur-sm p-6 md:p-8">
      {activeTab === TABS.MARS.id && <MarsTab />}
      {activeTab === TABS.MOON.id && <MoonTab />}
    </div>
  </main>
</div>
```
Existing imports in App.jsx: `useState`, `TabBar`, `StarField`, `MarsTab`, `MoonTab`, `{ DEFAULT_TAB, TABS }`. `activeTab` state already exists.

Current src/components/StarField.jsx style block ends with `opacity: 0.6,` (the single value to change to 0.4).

Components available from Plan 02 (default exports):
  src/components/AtmosphericBackdrop.jsx — `<AtmosphericBackdrop activeTab={...} />`
  src/components/BodyHero.jsx           — `<BodyHero activeTab={...} />`

Baseline for IMG-03 budget: v1.0 JS ≈ 211 KB; per-tab added WebP image weight must be ≤ ~300 KB on the WebP path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce StarField opacity so the backdrop reads through</name>
  <files>src/components/StarField.jsx</files>
  <read_first>
    - src/components/StarField.jsx (current state — the style block ends with `opacity: 0.6,`)
    - .planning/phases/07-hero-atmospheric-backdrop/07-UI-SPEC.md (section "Layered Z-Order Contract" — opacity reduced to ~0.4)
  </read_first>
  <action>
    In src/components/StarField.jsx, change the single line `opacity: 0.6,` to `opacity: 0.4,` (D-09). This is the only change to this file. Do NOT alter the backgroundImage, backgroundSize, className, aria-hidden, or any other property. The star field must still render — just dimmer — so it reads layered over the new AtmosphericBackdrop without washing the body image out.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "opacity: 0.4," src/components/StarField.jsx && ! grep -q "opacity: 0.6," src/components/StarField.jsx && grep -q 'pointer-events-none fixed inset-0 z-0' src/components/StarField.jsx && echo STARFIELD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/StarField.jsx` contains `opacity: 0.4,`
    - `src/components/StarField.jsx` does NOT contain `opacity: 0.6,`
    - The className `pointer-events-none fixed inset-0 z-0 bg-space-950` is unchanged (grep present)
    - `aria-hidden="true"` still present; backgroundImage radial-gradient block unchanged
  </acceptance_criteria>
  <done>StarField renders at 0.4 opacity; all other StarField behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AtmosphericBackdrop + BodyHero into App.jsx with correct z-order and glass panel</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (current full file — confirm existing imports and the exact data panel div className)
    - .planning/phases/07-hero-atmospheric-backdrop/07-PATTERNS.md (section "src/App.jsx" — target mount pattern lines 213-251)
    - .planning/phases/07-hero-atmospheric-backdrop/07-UI-SPEC.md (section "App.jsx Layout Integration" — locked structure)
  </read_first>
  <action>
    Make three edits to src/App.jsx:

    EDIT 1 — add two imports alongside the existing component imports (after the StarField import line):
    ```jsx
    import AtmosphericBackdrop from './components/AtmosphericBackdrop.jsx'
    import BodyHero from './components/BodyHero.jsx'
    ```

    EDIT 2 — mount AtmosphericBackdrop BEFORE StarField (so StarField, which follows it in DOM order, paints above it at the same z-0 layer — D-09). Replace the line `<StarField />` with:
    ```jsx
      <AtmosphericBackdrop activeTab={activeTab} />
      <StarField />
    ```

    EDIT 3 — insert BodyHero between the TabBar div and the data panel div, AND change the data panel's two glass classes (D-07). Replace this exact existing block:
    ```jsx
        <div className="mb-8 md:mb-10">
          <TabBar activeTab={activeTab} onTabChange={setActiveTab} />
        </div>

        <div className="rounded-lg border border-slate-800/60 bg-slate-950/40 backdrop-blur-sm p-6 md:p-8">
    ```
    with:
    ```jsx
        <div className="mb-8 md:mb-10">
          <TabBar activeTab={activeTab} onTabChange={setActiveTab} />
        </div>

        <BodyHero activeTab={activeTab} />

        <div className="rounded-lg border border-slate-800/60 bg-slate-950/30 backdrop-blur-md p-6 md:p-8">
    ```

    The two data-panel changes are: `bg-slate-950/40` → `bg-slate-950/30` (more translucent) and `backdrop-blur-sm` → `backdrop-blur-md` (more blur) — D-07 glassmorphism.

    IMPORTANT (D-06 guardrail): these `/30` + `blur-md` values are the TARGET look but they are subject to the contrast check in Task 3. If Task 3's contrast measurement fails AA, you WILL come back and reverse these toward `/40` (or `/60`) and `backdrop-blur-sm`. Leave `<main className="relative z-10 ...">` unchanged — content z-10 over the z-0 layers is the locked stacking contract.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "import AtmosphericBackdrop from './components/AtmosphericBackdrop.jsx'" src/App.jsx && grep -q "import BodyHero from './components/BodyHero.jsx'" src/App.jsx && grep -q "<AtmosphericBackdrop activeTab={activeTab} />" src/App.jsx && grep -q "<BodyHero activeTab={activeTab} />" src/App.jsx && grep -Eq "bg-slate-950/(30|40|60)" src/App.jsx && grep -q "backdrop-blur-md" src/App.jsx || grep -q "backdrop-blur-sm" src/App.jsx && grep -q 'relative z-10' src/App.jsx && awk '/<AtmosphericBackdrop/{a=NR} /<StarField \/>/{s=NR} END{exit !(a&&s&&a<s)}' src/App.jsx && echo APP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.jsx` imports `AtmosphericBackdrop` from `./components/AtmosphericBackdrop.jsx` and `BodyHero` from `./components/BodyHero.jsx`
    - `src/App.jsx` contains `<AtmosphericBackdrop activeTab={activeTab} />` and `<BodyHero activeTab={activeTab} />`
    - AtmosphericBackdrop appears BEFORE StarField in DOM order (awk line-number check: AtmosphericBackdrop line < StarField line)
    - BodyHero appears AFTER the TabBar div and BEFORE the data panel div (manual read confirms ordering; both grep-present)
    - The data panel div uses a `bg-slate-950/NN` class where NN ∈ {30,40,60} and a `backdrop-blur-md` or `backdrop-blur-sm` class (final value set by Task 3 contrast outcome)
    - `<main` still carries `relative z-10` (content stacking contract intact)
    - `npm run build` exits 0 (run as part of Task 3)
  </acceptance_criteria>
  <done>Both new components are mounted in the locked z-order; the data panel has the glass treatment (pending contrast confirmation in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Verify build, measure CLS + per-tab WebP weight, and enforce the WCAG AA contrast guardrail</name>
  <files>(verification + conditional re-tune of src/App.jsx / src/components/AtmosphericBackdrop.jsx)</files>
  <read_first>
    - .planning/phases/07-hero-atmospheric-backdrop/07-UI-SPEC.md (section "Accessibility & Contrast Guardrail" — targets + tuning levers in order of preference)
    - src/App.jsx (current data panel classes — the opacity tuning lever)
    - src/components/AtmosphericBackdrop.jsx (current filter brightness — the brightness tuning lever)
  </read_first>
  <action>
    Run the three live measurements that gate this phase. Apply the D-06 tuning levers if contrast fails, then re-verify.

    Step 1 — production build resolves everything:
    ```
    npm run build
    ```
    Must exit 0. Then measure the per-tab WebP weight from the build output (IMG-03):
    ```
    ls -l dist/assets/ | grep -iE 'mars-1600.*\.webp|moon-1600.*\.webp'
    ```
    The larger single body WebP is the per-tab added image weight (each tab loads ONE body's WebP). It MUST be ≤ ~300 KB (and per Plan 01 each is already ≤ 150 KB). Record both sizes in SUMMARY.

    Step 2 — CONTRAST CHECK (HERO-04 / D-06 — non-negotiable). The data panel uses white/near-white text (`text-slate-100`, data card values) over `bg-slate-950/30 backdrop-blur-md` sitting on top of the blurred backdrop. Verify WCAG AA:
    - Body text (data card values, prose): ≥ 4.5:1
    - Large/bold headings: ≥ 3.0:1
    Method (pick the most reliable available):
    (a) Compute the effective panel background color: slate-950 (#020617 ≈ rgb 2,6,23) at 30% over a backdrop dimmed to brightness 35%. The *worst case* is the brightest backdrop pixel showing through the 30% panel. Use a contrast calculator (e.g. a quick node script with the WCAG relative-luminance formula) comparing white (#ffffff) and the lightest plausible composited panel color.
    (b) If a headless measurement is impractical, the human-verify checkpoint (Task 4) is the contrast backstop — but you MUST still attempt the computed check and record the ratio in SUMMARY.

    IF contrast fails AA, apply tuning levers IN THIS ORDER (07-UI-SPEC "Tuning Levers"), re-checking after each:
    1. In src/components/AtmosphericBackdrop.jsx, reduce filter `brightness(35%)` toward `brightness(25%)`.
    2. In src/App.jsx, increase panel opacity: `bg-slate-950/30` → `bg-slate-950/40` (or `/60`).
    3. In src/components/AtmosphericBackdrop.jsx, strengthen the bottom gradient (raise the `transparent 40%` stop toward `transparent 60%`) to darken the lower viewport.
    Record the FINAL winning values in SUMMARY and note which levers (if any) were applied. Per D-06, legibility wins over the immersive target look.

    Step 3 — CLS sanity (IMG-02). Confirm the hero reserves space and no layout shift occurs:
    - Confirm in src/components/BodyHero.jsx the container has `aspect-[16/7]` and the `<img>` has explicit `width={1600} height={700}` (grep — these guarantee reserved dimensions).
    - In `npm run dev`, load the app; the data panel must NOT jump downward when the hero image finishes loading (the LQIP and reserved aspect ratio hold the space). Record observed behavior in SUMMARY (CLS ≈ 0).

    Re-run `npm run build` after any tuning edits; it must still exit 0.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run build >/tmp/p7-03-build.log 2>&1 && echo "BUILD_OK" && ls -l dist/assets/ | grep -iE 'mars-1600.*\.webp|moon-1600.*\.webp' && grep -q 'aspect-\[16/7\]' src/components/BodyHero.jsx && grep -q "width={1600}" src/components/BodyHero.jsx && grep -q "height={700}" src/components/BodyHero.jsx && for f in $(ls dist/assets/*1600*.webp 2>/dev/null); do b=$(wc -c < "$f"); echo "$f=$b"; [ "$b" -le 307200 ] || { echo "OVER BUDGET: $f"; exit 1; }; done && echo PERF_CLS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (after any tuning edits)
    - `dist/assets/` contains a mars `*1600*.webp` and a moon `*1600*.webp`, each ≤ 307200 bytes (~300 KB per-tab budget, IMG-03)
    - `src/components/BodyHero.jsx` still has `aspect-[16/7]`, `width={1600}`, `height={700}` (CLS reservation, IMG-02)
    - SUMMARY records: the computed (or human-confirmed) contrast ratio for data body text (target ≥ 4.5:1) and headings (≥ 3.0:1), and which tuning levers were applied (if any)
    - If tuning was applied: the final `brightness(NN%)` in AtmosphericBackdrop.jsx and the final `bg-slate-950/NN` in App.jsx are recorded in SUMMARY and consistent with the shipped files
    - SUMMARY records observed CLS behavior (data panel does not jump when hero loads)
  </acceptance_criteria>
  <done>Build is green, per-tab WebP weight is within budget, contrast meets AA (after tuning if needed), and CLS ≈ 0 is confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — both tabs show correct backdrop + hero, swap works, data legible</name>
  <what-built>
    AtmosphericBackdrop (full-bleed blurred/dimmed body backdrop) and BodyHero (sharp 16/7 framed hero with LQIP blur-up and "Artist's impression" label) are mounted in App.jsx in the locked z-order (backdrop → StarField at 0.4 opacity → content z-10). The data panel has glassmorphism (bg-slate-950/30 backdrop-blur-md, or the tuned value from Task 3). Per-tab WebP weight is within the ~300 KB budget and contrast was computed to meet WCAG AA.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local URL (typically http://localhost:5173).
    2. MARS tab (default): confirm a sharp Mars hero sits between the tab toggle and the data panel (~16/7, rounded corners, soft fade into the panel below), and a blurred/dimmed Mars backdrop fills the page behind the content. Confirm the "Artist's impression" label is visible in the hero's bottom-right corner.
    3. Click MOON: confirm BOTH the hero AND the full-bleed backdrop swap to the Moon (HERO-03). Switch back to Mars and confirm both swap back.
    4. LEGIBILITY (HERO-04 — the critical check): on both tabs, confirm every data card's value and label text, the freshness timestamps, headings, and tooltips are crisp and fully readable over the backdrop — no washed-out or low-contrast text. Compare against your memory of v1.0; there must be NO legibility regression.
    5. LAYOUT SHIFT (IMG-02): hard-refresh (Cmd+Shift+R). Watch the hero area — it should reserve its space immediately (a brief blur-up is fine) and the data panel below must NOT jump downward when the full image loads.
    6. STARFIELD: confirm the star texture still reads faintly layered over the backdrop (not gone, not overpowering).
    If any data text is hard to read, that is a HERO-04 failure — describe which text/tab and the executor will apply the D-06 tuning levers (darker backdrop / more opaque panel) and ask you to re-verify.
  </how-to-verify>
  <resume-signal>Type "approved" if both tabs render correctly, the swap works, and all data text is fully legible with no layout shift — or describe the specific issue (which tab, which text, or what shifted) so the tuning levers can be applied.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- Both tabs render the correct backdrop + hero; switching swaps both layers (HERO-03).
- Data text passes WCAG AA over the backdrop; no regression vs v1.0 (HERO-04).
- CLS ≈ 0 — hero reserves space, no panel jump on image load (IMG-02).
- Per-tab WebP weight ≤ ~300 KB (IMG-03).
- StarField at 0.4 opacity; content at z-10 over z-0 backdrop/starfield.
- Human checkpoint approved.
</verification>

<success_criteria>
- HERO-03: Switching Mars ↔ Moon swaps both the hero and the backdrop to the matching body, synchronized via the shared activeTab prop.
- HERO-04: Data panel and card text remain high-contrast and fully legible over the backdrop — no contrast regression vs v1.0 (AA verified; D-06 guardrail enforced).
- IMG-03: Added per-tab image weight stays within the ~300 KB WebP budget (measured from dist/).
- (Phase-level) HERO-01/HERO-02/IMG-02 are now live in the running app via the App.jsx integration.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backdrop layer → data legibility | The dimmed/blurred image compositing under the glass panel could reduce text contrast below AA |
| activeTab state → dual-layer swap | A single state value drives two components; desync would show mismatched body backdrop vs hero |
| committed assets → production bundle | Image weight entering the served bundle could regress load performance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information Disclosure (illegible data = degraded trust in real data) | data panel over backdrop | mitigate | Task 3 computes WCAG AA contrast and applies D-06 tuning levers (darker backdrop / more opaque panel) until body text ≥ 4.5:1; Task 4 human-verify is the final legibility backstop (HERO-04) |
| T-07-11 | Tampering (layer desync) | activeTab prop → backdrop + hero | mitigate | Both components receive the SAME `activeTab={activeTab}` prop from one state source; Task 4 step 3 explicitly verifies both layers swap together |
| T-07-12 | DoS (perf regression on load) | dist WebP weight | mitigate | Task 3 asserts each per-tab WebP ≤ 307200 bytes from `dist/assets/`; budget already enforced at source in Plan 01 |
| T-07-13 | Tampering (CSS filter/inline style injection) | AtmosphericBackdrop filter, panel classes | accept | All filter/opacity values are static literals edited by the executor only; no runtime user input flows into CSS — inert. Tuning edits are source-controlled and re-verified by build. |
| T-07-14 | Repudiation (CLS shifting content under user) | hero image load | mitigate | `aspect-[16/7]` + explicit img `width/height` reserve space; Task 3 + Task 4 step 5 confirm no panel jump on load (IMG-02, CLS ≈ 0) |
</threat_model>

<output>
After completion, create `.planning/phases/07-hero-atmospheric-backdrop/07-03-SUMMARY.md`. Document: final data-panel and backdrop-filter values (and which D-06 tuning levers were applied, if any), the computed/confirmed contrast ratios, the measured per-tab WebP weights from dist/, observed CLS behavior, and the human checkpoint outcome. This is the last plan of Phase 7 — note phase completion and that Phase 8 (Motion & Transitions) will animate these now-static layers.
</output>
