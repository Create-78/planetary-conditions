---
phase: 06-vercel-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
tags: [deployment, vercel, github, checkpoint, user-action]
user_setup:
  - service: github
    why: "Vercel imports the project from a GitHub repo; auto-deploy on push to main requires the repo to be on GitHub"
    account_setup:
      - "Create a GitHub account at https://github.com if you don't have one"
      - "Create a new repository named 'planetary-conditions' (visibility: public recommended; private also works)"
    dashboard_config:
      - task: "Create the GitHub repo (empty — no README/license/gitignore from GitHub's side, since the local repo already has them)"
        location: "https://github.com/new"
  - service: vercel
    why: "Hosts the static SPA; auto-deploys from main; provides the production URL"
    account_setup:
      - "Create a Vercel account at https://vercel.com (free Hobby tier sufficient) — sign in with the GitHub account so Vercel can import the repo"
    env_vars:
      - name: VITE_NASA_API_KEY
        source: "Register a real key at https://api.nasa.gov (recommended — 1000 req/hr) or use 'DEMO_KEY' (30 req/hr, will rate-limit)"
        scope: "Production + Preview (Vercel dashboard → Project → Settings → Environment Variables)"
    dashboard_config:
      - task: "Import the planetary-conditions GitHub repo as a new Vercel project (Vite framework auto-detected)"
        location: "https://vercel.com/new"
      - task: "Set VITE_NASA_API_KEY env var before clicking Deploy"
        location: "Project → Settings → Environment Variables"
  - service: nasa-api
    why: "DONKI events endpoint requires an API key; DEMO_KEY rate-limits at 30 req/hr"
    account_setup:
      - "Register at https://api.nasa.gov — fills a form, key arrives via email instantly (free; 1000 req/hr)"
    env_vars:
      - name: VITE_NASA_API_KEY
        source: "https://api.nasa.gov (email delivery)"
        scope: "Set in Vercel dashboard, not committed to repo"
must_haves:
  truths:
    - "User has a GitHub repo with the project's code on the main branch"
    - "Vercel has a project linked to the GitHub repo and configured with VITE_NASA_API_KEY"
    - "Production URL is reachable in a browser and loads the Planetary Conditions UI"
    - "Mars tab on production loads real MAAS2 data (8 DataCards render values, not em-dashes)"
    - "Moon tab on production loads real data across all three sections (lunar context, SWPC, DONKI)"
    - "Browser DevTools Network panel shows 7 successful 200 responses (1 MAAS2 + 3 SWPC + 3 DONKI) from the production origin, with zero CORS errors in console"
    - "DONKI requests on production use the registered NASA API key, not DEMO_KEY"
    - "Pushing a new commit to main triggers an auto-deploy in Vercel (verified by observing build status in dashboard)"
    - "README.md displays the actual production URL (Live demo line replaced from '(coming soon)' to the real URL)"
    - "PROJECT.md marks Vercel deployment decision as Validated with date and URL"
    - "REQUIREMENTS.md marks DEPLOY-01..DEPLOY-04 complete"
  artifacts:
    - path: "README.md"
      provides: "Public-facing intro with live demo URL"
      contains: "**Live demo:** https://"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table with validated Vercel deployment row"
      contains: "Vercel deployment"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEPLOY-01..DEPLOY-04 marked complete in traceability table"
      contains: "DEPLOY-01"
  key_links:
    - from: "GitHub repo main branch"
      to: "Vercel project"
      via: "Vercel's GitHub app auto-deploy on push"
      pattern: "auto-deploy"
    - from: "Vercel build process"
      to: "VITE_NASA_API_KEY env var"
      via: "Dashboard Environment Variables → Production + Preview scopes"
      pattern: "VITE_NASA_API_KEY"
    - from: "Production URL"
      to: "MAAS2 + NOAA SWPC + NASA DONKI"
      via: "Client-side fetch with no proxy (all 3 are CORS-open)"
      pattern: "CORS-open"
    - from: "README.md"
      to: "Production URL"
      via: "Live demo line — populated post-deploy in Task 7"
      pattern: "\\*\\*Live demo:\\*\\* https://"
---

<objective>
Hand the wheel to the user for the **GitHub push, Vercel import, env var configuration, first deploy, CORS verification, and end-to-end smoke test** — the six user-gated steps that actually put the app on the internet. Once the user confirms the production URL and that both tabs render live data, Claude executes the final Task 7 autonomously: updates README.md with the real Live demo URL, marks PROJECT.md's Vercel decision as Validated, and ticks DEPLOY-01..DEPLOY-04 to Complete in REQUIREMENTS.md.

Purpose: This is the **first non-autonomous plan in the entire milestone** (D-22). Claude does NOT push to GitHub, does NOT run `vercel link` / `vercel --prod`, and does NOT modify Vercel project state — per CLAUDE.md guardrails and D-08. Claude's job here is to (a) gate cleanly on each user action with verifiable resume signals, (b) confirm preconditions before advancing, and (c) execute the docs-update sweep autonomously only after the user supplies the confirmed production URL.

Output:
- A live `https://planetary-conditions(-<suffix>).vercel.app` URL (or whatever Vercel assigned) serving the production build
- README.md "Live demo" line replaced from `(coming soon)` to the actual URL
- PROJECT.md Vercel deployment decision marked "✓ Good" with date + URL
- REQUIREMENTS.md DEPLOY-01..DEPLOY-04 ticked complete in the traceability table
- All 4 ROADMAP Phase 6 success criteria observable (auto-deploy, CORS verified, real key in use, prod preview matches local)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-vercel-deployment/06-CONTEXT.md
@.planning/phases/06-vercel-deployment/06-01-PLAN.md
@CLAUDE.md
@Discussion.md
@DEPLOYMENT.md
@README.md

<interfaces>
<!-- Critical references for the executor handling the checkpoints. -->

Vercel dashboard nav paths (verbatim — use these when narrating to the user):
- Import: https://vercel.com/new
- Env vars: Project → Settings → Environment Variables
- Deployments / build log: Project → Deployments
- Redeploy: Deployments → latest → ⋯ menu → Redeploy

NASA API key registration: https://api.nasa.gov (single-page form, instant email)

The 7 production upstream fetches the user verifies (D-12 — CONTEXT mentions "6 fetches" but actual count is 7; reconcile to 7):
1. `https://api.maas2.apollorion.com/` (MAAS2 latest sol)
2. `https://services.swpc.noaa.gov/products/solar-wind/plasma-2-hour.json`
3. `https://services.swpc.noaa.gov/products/solar-wind/mag-2-hour.json`
4. `https://services.swpc.noaa.gov/products/noaa-planetary-k-index.json`
5. `https://api.nasa.gov/DONKI/CME?api_key=<KEY>&startDate=...`
6. `https://api.nasa.gov/DONKI/FLR?api_key=<KEY>&startDate=...`
7. `https://api.nasa.gov/DONKI/GST?api_key=<KEY>&startDate=...`

Current README.md "Live demo" line (placeholder to be replaced in Task 7):
```
**Live demo:** (coming soon — will be updated when first Vercel deploy completes)
```

Current PROJECT.md Vercel decision row (to be updated in Task 7):
```
| Vercel deployment | Zero-config GitHub deploys, env vars, free tier sufficient | — Pending |
```

Current REQUIREMENTS.md DEPLOY-* status (to be flipped in Task 7):
```
| DEPLOY-01 | Phase 6: Vercel Deployment | Pending |
| DEPLOY-02 | Phase 6: Vercel Deployment | Pending |
| DEPLOY-03 | Phase 6: Vercel Deployment | Pending |
| DEPLOY-04 | Phase 6: Vercel Deployment | Pending |
```

And in the requirements list section itself:
```
- [ ] **DEPLOY-01**: Vercel project connected ...
- [ ] **DEPLOY-02**: VITE_NASA_API_KEY set in Vercel dashboard ...
- [ ] **DEPLOY-03**: CORS verified for all four data sources ...
- [ ] **DEPLOY-04**: Production preview shows both tabs ...
```

(Note: REQUIREMENTS.md says "all four data sources" — there are actually three upstream services (MAAS2, NOAA SWPC, NASA DONKI), but they emit 7 endpoint hits. Both phrasings are referenced upstream; do not "fix" the requirement text — only flip the `[ ]` to `[x]`.)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 — USER ACTION: Push the project to GitHub (D-08)</name>
  <read_first>
    - DEPLOYMENT.md sections "Prerequisites" + "First-time deployment → step 2: Push the repo to GitHub"
    - CLAUDE.md guardrail: Claude does NOT run `git push` autonomously
  </read_first>
  <what-built>
    Plan 06-01 has verified the repo is hygiene-clean: no committed NASA API keys, `.env` not tracked, `dist/` not tracked, `npm run build` exits 0. The repo is ready to push.
  </what-built>
  <user_action>
**You (the user) do this:**

1. Go to [https://github.com/new](https://github.com/new) and create a new **empty** repository named `planetary-conditions`. Do NOT initialize with README / .gitignore / license (the local repo already has all three).
   - Visibility: public recommended (educational/portfolio project), private also works.

2. Note the repo URL — it will be `https://github.com/<your-github-username>/planetary-conditions.git`.

3. From the project root (where this README sits), run:
   ```bash
   git remote add origin https://github.com/<your-github-username>/planetary-conditions.git
   git branch -M main
   git push -u origin main
   ```

4. Verify the push succeeded by visiting the repo URL in a browser — you should see all the project files including `DEPLOYMENT.md`.
  </user_action>
  <how-to-verify>
    After the push, in your terminal run:
    ```bash
    git remote -v
    git ls-remote origin main
    ```
    Confirm `origin` points at your GitHub URL and `ls-remote` returns a SHA matching `git rev-parse HEAD` locally.
  </how-to-verify>
  <resume-signal>
    Type "pushed" with your GitHub username (e.g., `pushed willmcdermott`) so Claude can record the repo URL in the SUMMARY. If anything failed during the push (e.g., authentication, branch protection), paste the error and Claude will help diagnose before continuing.
  </resume-signal>
  <claude-followup-after-resume>
    Once user resumes, Claude runs (read-only verification):
    ```bash
    git remote -v
    git ls-remote origin main
    git rev-parse HEAD
    ```
    Confirm `origin` is set and remote HEAD matches local HEAD. Record the GitHub URL for the SUMMARY. Do NOT proceed to Task 2 until verification is clean.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - `git remote -v` shows an `origin` remote pointing at `github.com/<user>/planetary-conditions`
    - `git ls-remote origin main` returns a SHA that matches local `git rev-parse HEAD`
    - The repo is browsable in a web browser at `https://github.com/<user>/planetary-conditions`
    - DEPLOYMENT.md and README.md (with Plan 06-01 changes) are visible in the GitHub UI
  </acceptance_criteria>
  <done>
    Repo on GitHub, main branch pushed, remote HEAD matches local HEAD, GitHub URL captured for SUMMARY.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 — USER ACTION: Import the repo as a new Vercel project (D-10, D-11)</name>
  <read_first>
    - DEPLOYMENT.md "First-time deployment → step 3: Import the project in Vercel"
    - 06-CONTEXT.md D-10 (two equivalent paths — Dashboard vs CLI)
  </read_first>
  <what-built>
    The repo is on GitHub. Vercel needs to be linked to it before it can build and serve the production URL.
  </what-built>
  <user_action>
**You (the user) do this — pick one path:**

**Path A (recommended — dashboard import):**
1. Go to [https://vercel.com/new](https://vercel.com/new).
2. If this is your first time, sign in with GitHub when prompted (grant the Vercel GitHub app permission to read your repos — accept the default scope, which can be "All repositories" or "Only select repositories" with `planetary-conditions` selected).
3. Click **Add New… → Project**.
4. Find `planetary-conditions` in the repo list and click **Import**.
5. On the configuration screen, **DO NOT click Deploy yet** — Task 3 sets the env var first. Leave the screen open.
6. Confirm the auto-detected settings match exactly:
   - Framework Preset: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

**Path B (CLI):**
1. From the project root:
   ```bash
   npm install -g vercel
   vercel login
   vercel link
   ```
2. Answer the prompts:
   - Set up "planetary-conditions"? **Y**
   - Which scope? Your personal account (or team, if applicable)
   - Link to existing project? **N**
   - What's your project's name? `planetary-conditions`
   - In which directory is your code located? `./`
3. After `vercel link` completes, a `.vercel/` directory will be created locally (already gitignored — confirm with `cat .gitignore | grep -i vercel`; if no entry, you may want to add `.vercel` to `.gitignore` to be safe, but `.vercel` is not typically tracked by default).
4. Do NOT run `vercel --prod` yet — env var setup is Task 3.
  </user_action>
  <how-to-verify>
    **Path A:** The Vercel "Configure Project" screen for `planetary-conditions` is open in your browser, showing the auto-detected Vite settings.

    **Path B:** A `.vercel/project.json` file exists locally containing `projectId` and `orgId`.

    ```bash
    # Path B verification
    [ -f .vercel/project.json ] && cat .vercel/project.json
    ```
  </how-to-verify>
  <resume-signal>
    Type "linked A" if you used the dashboard (and the configure screen is open) or "linked B" if you used the CLI. Include the Vercel project name shown (defaults to `planetary-conditions` but may have a suffix if the name was taken).
  </resume-signal>
  <claude-followup-after-resume>
    Claude confirms which path was used. For Path B, Claude runs:
    ```bash
    [ -f .vercel/project.json ] && echo ".vercel linked" || echo "MISSING .vercel/project.json"
    grep -q '^\.vercel' .gitignore || echo "WARN: .vercel not in .gitignore (Vercel CLI typically doesn't require it but defense-in-depth)"
    ```
    If `.vercel` is not gitignored AND the file exists tracked, Claude surfaces this for the user to add to `.gitignore` before next commit. Do NOT modify `.gitignore` autonomously — user authorizes.

    For Path A, no local file changes — Claude just confirms the project name and moves to Task 3.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - Vercel knows about the GitHub repo (either via dashboard import screen or via local `.vercel/project.json`)
    - Vite framework preset is confirmed (auto-detected — D-03 honored, no `vercel.json` added)
    - The project name is captured (defaults to `planetary-conditions`; may have `-<suffix>` if collision)
    - No deploy has been triggered yet (env var still pending — Task 3)
  </acceptance_criteria>
  <done>
    Vercel-GitHub linkage established; auto-detected build settings confirmed; env var configuration is next.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 — USER ACTION: Register NASA API key and set VITE_NASA_API_KEY in Vercel (DEPLOY-02; D-04, D-05)</name>
  <read_first>
    - DEPLOYMENT.md "First-time deployment → step 4: Set the NASA API key in Vercel"
    - 06-CONTEXT.md D-04, D-05 (Production + Preview scopes; register real key not DEMO_KEY)
    - REQUIREMENTS.md DEPLOY-02
  </read_first>
  <what-built>
    Vercel knows about the repo but doesn't yet have the NASA API key. Without it, DONKI requests will fall back to `DEMO_KEY` (30 req/hr) and rate-limit quickly under any real traffic.
  </what-built>
  <user_action>
**You (the user) do this:**

1. **Register a NASA API key** (recommended — skip if you already have one):
   - Go to [https://api.nasa.gov](https://api.nasa.gov)
   - Fill the form (first name, last name, email — that's it)
   - Click **Signup**
   - The key arrives in your email within seconds — a 40-character alphanumeric string like `aBcD1234efGh5678ijKl9012mnOp3456qrSt7890`

2. **Set the env var in Vercel:**
   - In the Vercel project dashboard, go to **Project → Settings → Environment Variables**
   - Click **Add New**
   - Fill in:
     - **Name:** `VITE_NASA_API_KEY` (exact — capital VITE_, underscores)
     - **Value:** paste your NASA API key from step 1
     - **Environments:** check **☑ Production** AND **☑ Preview**. Leave **☐ Development** unchecked (local `.env` handles dev).
   - Click **Save**

3. Confirm the var appears in the Environment Variables list with the right scopes (Production + Preview).

> Do NOT add `VITE_NASA_API_KEY` to a `.env` file in the repo and push it — env vars live in Vercel's dashboard, not in git.
  </user_action>
  <how-to-verify>
    In Vercel: **Project → Settings → Environment Variables** — you see one row:
    | Key | Value | Environments |
    |-----|-------|--------------|
    | `VITE_NASA_API_KEY` | `••••••••` (hidden) | Production, Preview |
  </how-to-verify>
  <resume-signal>
    Type "env set" once the var is saved in Vercel's dashboard with Production + Preview scopes. (No need to share the key value — Claude does not need to see it.)
  </resume-signal>
  <claude-followup-after-resume>
    Claude has no way to verify Vercel dashboard state directly (no CLI inspection in this plan per the guardrail against state-modifying Vercel CLI commands). Trust the user's confirmation and proceed to Task 4.

    Optional read-only check the user can run themselves:
    ```bash
    vercel env ls
    ```
    Should show `VITE_NASA_API_KEY` on Production + Preview. If the user runs this and pastes the output, Claude validates the row exists.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - `VITE_NASA_API_KEY` exists in Vercel's Environment Variables for the project
    - Scopes include Production AND Preview (Development optional/unchecked)
    - The value is a real NASA API key (not literal `DEMO_KEY`) — confirmed by the user
  </acceptance_criteria>
  <done>
    Env var configured; first build can now run with the real key embedded.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 — USER ACTION: Trigger first deploy and capture the production URL (DEPLOY-01)</name>
  <read_first>
    - DEPLOYMENT.md "First-time deployment → step 5: Trigger the first deploy"
    - 06-CONTEXT.md D-01, D-11 (auto-deploy on push; URL pattern `planetary-conditions.vercel.app` or with `-suffix`)
    - REQUIREMENTS.md DEPLOY-01
  </read_first>
  <what-built>
    Vercel is linked to the GitHub repo and has the env var. The first build needs to be triggered and observed.
  </what-built>
  <user_action>
**You (the user) do this:**

**If you used Path A (dashboard import) in Task 2:**
- Return to the Vercel "Configure Project" screen that's still open. Confirm the env var you added in Task 3 appears under **Environment Variables** (you may need to scroll or click the "Environment Variables" disclosure).
- Click the big **Deploy** button.

**If you used Path B (CLI) in Task 2:**
- Either trigger via GitHub:
  ```bash
  # Push any small change (or an empty commit) to trigger Vercel's GitHub integration:
  git commit --allow-empty -m "chore(deploy): trigger first Vercel deploy"
  git push origin main
  ```
- Or via Vercel CLI:
  ```bash
  vercel --prod
  ```

**Then:**
1. Watch the build progress in the Vercel dashboard (**Project → Deployments → latest**).
2. Expected duration: 30–90 seconds. Build log ends with something like `Build Completed in /vercel/output [<time>]` and `Deployment Completed`.
3. Vercel shows the production URL on the deployment card — usually `https://planetary-conditions.vercel.app` (or with a `-<suffix>` if the name was taken in your scope).
4. Copy the production URL.
  </user_action>
  <how-to-verify>
    - Vercel dashboard shows the deployment status as **Ready** (green checkmark) for the latest production deploy
    - Clicking the production URL opens the app in a new tab
    - The app renders (at minimum) the dark cinematic shell with the Mars/Moon tabs
  </how-to-verify>
  <resume-signal>
    Type "deployed <URL>" where `<URL>` is the production URL Vercel assigned (e.g., `deployed https://planetary-conditions.vercel.app`). Claude needs this exact URL for Task 7's README update.

    If the build failed, paste the build log error and Claude will help diagnose before continuing.
  </resume-signal>
  <claude-followup-after-resume>
    Claude records the production URL exactly as the user provided it (preserving any `-suffix` Vercel assigned). Validation:
    ```bash
    # Validate URL shape only; do NOT fetch (avoid CORS/auth shenanigans, plus Task 5 has the user verify in a real browser)
    echo "$PRODUCTION_URL" | grep -qE '^https://planetary-conditions[a-z0-9-]*\.vercel\.app/?$'
    ```
    If the URL doesn't match the expected pattern, surface this to the user and confirm before proceeding — Vercel may have assigned a fully custom subdomain in rare cases, which is also fine.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - Production deploy completed with status "Ready" in Vercel
    - User has provided the exact production URL (captured in Claude's working memory for Task 7)
    - URL is reachable in a browser (renders something, even if Task 5 will catch CORS issues)
  </acceptance_criteria>
  <done>
    First deploy live; production URL captured; ready for the CORS + smoke-test verification gates.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 — USER VERIFY: CORS for all 7 upstream fetches from production (DEPLOY-03)</name>
  <read_first>
    - DEPLOYMENT.md "First-time deployment → step 6: Verify the production URL"
    - 06-CONTEXT.md D-12 (verification procedure), D-13 (proxy contingency deferred)
    - REQUIREMENTS.md DEPLOY-03
  </read_first>
  <what-built>
    The production URL is live. CORS behavior in production may differ from localhost (different origin, different referrer headers), so every upstream fetch must be verified from the deployed origin.
  </what-built>
  <how-to-verify>
**You (the user) do this:**

1. Open the production URL in a **regular browser tab** (not incognito — extensions are fine, but a clean Chrome/Firefox/Safari profile is ideal).
2. Open **DevTools** (F12 / Cmd+Opt+I) → **Network** tab → filter to **Fetch/XHR**.
3. Hard-refresh (Cmd+Shift+R / Ctrl+F5) so all fetches are observable from clean state.
4. **On the Mars tab** (default landing), confirm:
   - **1 GET** to `https://api.maas2.apollorion.com/` (or `https://api.maas2.apollorion.com/?something`) — status **200**, response body is JSON with `sol`, `terrestrial_date`, etc.
5. **Switch to the Moon tab**, confirm:
   - **3 GETs** to `https://services.swpc.noaa.gov/products/solar-wind/`:
     - `plasma-2-hour.json` — 200
     - `mag-2-hour.json` — 200
     - And `https://services.swpc.noaa.gov/products/noaa-planetary-k-index.json` — 200
   - **3 GETs** to `https://api.nasa.gov/DONKI/`:
     - `CME?api_key=...` — 200
     - `FLR?api_key=...` — 200
     - `GST?api_key=...` — 200
6. **Inspect a DONKI request's URL** (click any of the three DONKI rows in Network → Headers → Request URL). The query string MUST contain `api_key=<your-real-40-char-key>`. **MUST NOT** contain `api_key=DEMO_KEY`. If it shows `DEMO_KEY`, the env var didn't propagate — go to Vercel → Deployments → latest → ⋯ → **Redeploy**, then re-test after the redeploy finishes.
7. **Open the Console tab** in DevTools. Confirm **zero** errors matching `CORS policy: ...blocked...` or `Access-Control-Allow-Origin`.

**Total: 7 upstream fetches, all 200, zero CORS errors, all DONKI URLs use the real key.**
  </how-to-verify>
  <resume-signal>
    Type "cors verified" if all 7 fetches returned 200 with zero CORS errors AND DONKI uses the real key (not DEMO_KEY).

    Type "cors failed <details>" with specifics (which endpoint failed, what error appeared) if anything blocked. Claude will help diagnose — but per D-13, the Edge Function proxy fallback is NOT pre-built; it's only added if there's evidence of an unavoidable block.

    Type "demo_key still in use" if you see `api_key=DEMO_KEY` in DONKI URLs after redeploying — there's likely an env var naming typo (must be exactly `VITE_NASA_API_KEY`) or the redeploy hadn't completed before re-testing.
  </resume-signal>
  <claude-followup-after-resume>
    On "cors verified": record the 7 fetches as observed and move to Task 6. No code action needed.

    On "cors failed": STOP this plan. Do not advance to Task 6 or Task 7. Surface a diagnostic question: which endpoint, what's the exact error, did the same endpoint work locally? Recommend the user share the specific console error + the failing request's Response headers. Per D-13, the proxy is built only if evidence shows it's needed — first rule out env var typos, browser extensions, and corporate network proxies.

    On "demo_key still in use": guide user to (a) check the env var name spelling in Vercel dashboard (`VITE_NASA_API_KEY` exactly, no leading space, correct case), (b) confirm Production scope is checked, (c) trigger a manual Redeploy from the Deployments tab, (d) re-test after redeploy completes.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - DevTools Network panel shows all 7 upstream fetches returning HTTP 200 from the production origin
    - DevTools Console has zero CORS-related error messages
    - DONKI request URLs show `api_key=<real-key>` (40-char alphanumeric, not `DEMO_KEY`)
    - User explicitly confirms with `cors verified`
  </acceptance_criteria>
  <done>
    DEPLOY-03 satisfied: CORS verified end-to-end from the deployed origin for all three upstream services (MAAS2, NOAA SWPC, NASA DONKI).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 — USER VERIFY: Smoke test both tabs render live data end-to-end (DEPLOY-04)</name>
  <read_first>
    - DEPLOYMENT.md "First-time deployment → step 7: Smoke test both tabs"
    - 06-CONTEXT.md specifics (smoke test checklist)
    - REQUIREMENTS.md DEPLOY-04
  </read_first>
  <what-built>
    The production URL responds and CORS is verified. Final gate: confirm the rendered UI matches the local end-to-end experience — same data, same UX, no production-only regressions.
  </what-built>
  <how-to-verify>
**You (the user) do this on the production URL:**

**Mars tab:**
- All **8 DataCards** render numeric values (NOT em-dashes everywhere — at least 6 of 8 should have real values; some MAAS2 fields are sometimes legitimately null for certain sols, which is expected)
- Source attribution "From Curiosity Rover / REMS instrument" is visible somewhere on the tab
- Per-card "Last updated X mins ago" timestamps appear (each card has its own)
- Hovering any value shows a tooltip with an Earth-anchored explainer

**Moon tab — Section 1 (Lunar Context):**
- Current lunar phase name (e.g., "Waxing Gibbous"), phase percentage, day/night indicator
- Estimated surface temperature with the word "Estimated" visible in label or tooltip

**Moon tab — Section 2 (Space Weather):**
- 4 SWPC DataCards (Solar Wind Speed km/s, Density p/cm³, Bz nT, Kp Index)
- A Radiation Risk StatusBadge showing Low / Moderate / High (with appropriate color)

**Moon tab — Section 3 (Solar Event Alerts):**
- EITHER a scrollable list of AlertCards (CME/FLR/GST events with UTC times, severities)
- OR the empty-state copy: "No significant events in the past 7 days — conditions are calm."

**Overall:**
- No blank panels anywhere on either tab
- No console errors other than expected fetch wait times
- No "Data temporarily unavailable" banners (unless an upstream really is down — re-test in 5 minutes if so)
  </how-to-verify>
  <resume-signal>
    Type "smoke passed" if all sections rendered as described above.

    Type "smoke failed <section>" if any tab/section was blank, errored, or showed unexpected behavior — Claude will help diagnose before completing the phase.
  </resume-signal>
  <claude-followup-after-resume>
    On "smoke passed": all 4 ROADMAP Phase 6 success criteria are now observable. Proceed to Task 7 (the autonomous docs sweep).

    On "smoke failed": STOP. Do not advance to Task 7. Phase 6 cannot complete with a broken production deploy. Ask the user for specifics (which section, what error if any, whether console showed anything). This is the last gate before Claude marks DEPLOY-01..04 complete — accuracy matters.
  </claude-followup-after-resume>
  <acceptance_criteria>
    - Mars tab: 8 DataCards rendering, source attribution visible, tooltips work
    - Moon tab Section 1 (Lunar): phase name + percentage + day/night + estimated temp all present
    - Moon tab Section 2 (SWPC): 4 DataCards + Radiation Risk StatusBadge present
    - Moon tab Section 3 (DONKI): AlertCard list OR verbatim empty-state copy present
    - No blank panels, no full-page failures
    - User explicitly confirms with `smoke passed`
  </acceptance_criteria>
  <done>
    DEPLOY-04 satisfied: production preview matches local end-to-end. All 4 Phase 6 ROADMAP success criteria observably true.
  </done>
</task>

<task type="auto">
  <name>Task 7: Update README.md Live demo URL + PROJECT.md decision row + REQUIREMENTS.md DEPLOY-* status (D-18, D-20)</name>
  <files>
    - README.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - The user's confirmed production URL from Task 4's resume signal (e.g., `https://planetary-conditions.vercel.app`)
    - README.md (the current Live demo placeholder line)
    - .planning/PROJECT.md (Key Decisions table — Vercel deployment row)
    - .planning/REQUIREMENTS.md (Deployment section + Traceability table)
    - 06-CONTEXT.md D-18, D-19, D-20
  </read_first>
  <action>
**Precondition gate:** This task ONLY runs after Task 6 returned `smoke passed` AND Task 4 captured the production URL. If either is missing, STOP — surface to user that Task 7 cannot proceed without those two confirmations.

Capture the production URL the user provided in Task 4 (resume signal `deployed <URL>`). Call this `<PROD_URL>`. Example: `https://planetary-conditions.vercel.app`.

Today's date (per session context): `2026-05-21`. Call this `<TODAY>`.

**Edit A — README.md: replace Live demo placeholder.**

Use the Edit tool. Find:
```
**Live demo:** (coming soon — will be updated when first Vercel deploy completes)
```

Replace with:
```
**Live demo:** <PROD_URL>
```

(Substitute `<PROD_URL>` with the actual URL — no angle brackets in the final file.)

**Edit B — README.md: append a Recent Updates entry for today.**

Use the Edit tool. Find the line `### 2026-05-14` under `## Recent Updates`. Insert ABOVE it:
```
### 2026-05-21
- Production deploy live on Vercel — `<PROD_URL>`
- Phase 6 complete; all DEPLOY-01..DEPLOY-04 requirements satisfied
- README "Live demo" link populated; PROJECT.md Vercel decision validated

```

(Maintain the blank line between dated entries.)

**Edit C — README.md: bump the Status table "Last Updated" row.**

Use the Edit tool. Find:
```
| **Last Updated** | 2026-05-14 |
```

Replace with:
```
| **Last Updated** | 2026-05-21 |
```

Also update the Status row from `🟡 In Progress` and Phase row from `Pre-build — spec complete, scaffold pending` — but conservatively. Replace:
```
| **Overall** | 🟡 In Progress |
| **Phase** | Pre-build — spec complete, scaffold pending |
```
with:
```
| **Overall** | 🟢 Live on Vercel |
| **Phase** | v1.0 deployed |
```

**Edit D — PROJECT.md: validate the Vercel deployment decision row (D-20).**

Use the Edit tool. Find:
```
| Vercel deployment | Zero-config GitHub deploys, env vars, free tier sufficient | — Pending |
```

Replace with:
```
| Vercel deployment | Zero-config GitHub deploys, env vars, free tier sufficient | ✓ Good (2026-05-21, <PROD_URL>) |
```

**Edit E — REQUIREMENTS.md: flip DEPLOY-01..DEPLOY-04 to checked + update traceability table.**

Use the Edit tool four times (one per requirement). For each of DEPLOY-01..DEPLOY-04, find the line:
```
- [ ] **DEPLOY-0N**: ...
```
and replace `[ ]` with `[x]`. Preserve the rest of the line verbatim (including the trailing `**: ...` text).

Then in the Traceability table, for each of DEPLOY-01..DEPLOY-04, find the row:
```
| DEPLOY-0N | Phase 6: Vercel Deployment | Pending |
```
and replace `Pending` with `Complete (2026-05-21, 06-02)`.

**Edit F — PROJECT.md: move Vercel deployment from Active requirements list to Validated section.**

Use the Edit tool. In the Requirements > Active section, find:
```
- [ ] Vercel deployment with `VITE_NASA_API_KEY` env var (auto-deploy from `main`)
```

Remove that line entirely (delete it from Active).

Then in the Validated section, find:
```
(None yet — ship to validate)
```
Replace with:
```
- [x] Vercel deployment with `VITE_NASA_API_KEY` env var (auto-deploy from `main`) — validated 2026-05-21 at <PROD_URL>
```

(With `<PROD_URL>` substituted to the real URL.)

**Final sanity check before commit:**
- `grep -E '\*\*Live demo:\*\* https://' README.md` → must match
- `grep '✓ Good (2026-05-21' .planning/PROJECT.md` → must match
- `grep -E '\- \[x\] \*\*DEPLOY-0[1-4]\*\*' .planning/REQUIREMENTS.md | wc -l` → must equal 4
- `grep -E 'DEPLOY-0[1-4] \| Phase 6: Vercel Deployment \| Complete' .planning/REQUIREMENTS.md | wc -l` → must equal 4
  </action>
  <verify>
    <automated>
      set -e
      # README updated with real URL
      grep -qE '^\*\*Live demo:\*\* https://[a-z0-9-]+\.vercel\.app' README.md
      ! grep -q '\*\*Live demo:\*\* (coming soon' README.md
      grep -q '^### 2026-05-21' README.md
      grep -q '🟢 Live on Vercel' README.md
      grep -q 'v1.0 deployed' README.md
      grep -q '| \*\*Last Updated\*\* | 2026-05-21 |' README.md
      # PROJECT.md decision validated
      grep -q 'Vercel deployment.*✓ Good (2026-05-21' .planning/PROJECT.md
      # Vercel moved out of Active into Validated in PROJECT.md
      ! grep -q '^- \[ \] Vercel deployment with' .planning/PROJECT.md
      grep -qE '^- \[x\] Vercel deployment with .* validated 2026-05-21' .planning/PROJECT.md
      # REQUIREMENTS.md DEPLOY-* checked in the requirements list
      [ "$(grep -cE '^- \[x\] \*\*DEPLOY-0[1-4]' .planning/REQUIREMENTS.md | tr -d ' ')" = "4" ]
      # And in the traceability table
      [ "$(grep -cE '^\| DEPLOY-0[1-4] \| Phase 6: Vercel Deployment \| Complete \(2026-05-21, 06-02\)' .planning/REQUIREMENTS.md | tr -d ' ')" = "4" ]
      ! grep -qE '^\| DEPLOY-0[1-4] \| Phase 6: Vercel Deployment \| Pending' .planning/REQUIREMENTS.md
      # No source code modified by this task
      [ -z "$(git diff --name-only src/ 2>/dev/null)" ]
      # Build still green (defensive — doc-only changes should not affect build)
      npm run build > /tmp/06-02-final-build.log 2>&1
      grep -qE '✓ built in' /tmp/06-02-final-build.log
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md Live demo line shows the real `https://...vercel.app` URL the user provided (not the `(coming soon)` placeholder)
    - README.md has a new `### 2026-05-21` entry in Recent Updates mentioning the production deploy
    - README.md Status table shows `🟢 Live on Vercel` and `v1.0 deployed` and `Last Updated: 2026-05-21`
    - PROJECT.md Key Decisions table shows Vercel deployment row as `✓ Good (2026-05-21, <URL>)`
    - PROJECT.md Validated requirements section contains the Vercel deployment entry; Active no longer lists it
    - REQUIREMENTS.md has all four DEPLOY-01..DEPLOY-04 lines flipped from `[ ]` to `[x]` in the list
    - REQUIREMENTS.md traceability table shows all four DEPLOY-* rows as `Complete (2026-05-21, 06-02)`, none `Pending`
    - `src/**` has zero modifications (this task is docs-only)
    - `npm run build` still exits 0
  </acceptance_criteria>
  <done>
    Documentation reflects the live production deploy; all 4 deployment requirements marked complete; PROJECT.md decision validated. Phase 6 (and the entire v1 milestone) is shippable.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub remote | First push exposes the entire git history publicly (if repo is public). All pre-push hygiene done in Plan 06-01. |
| Vercel build env → client bundle | `VITE_NASA_API_KEY` is inlined into the served JS at build time — present in plaintext in the production bundle. |
| User's browser → upstream APIs | Production-origin CORS preflight crosses into upstream service trust boundaries (different from localhost preflight). |
| Claude → user input | Claude cannot verify Vercel dashboard state directly; relies on the user's resume signals (`pushed`, `linked`, `env set`, `deployed <URL>`, `cors verified`, `smoke passed`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | I (Information disclosure) | First `git push` to a public GitHub repo | mitigate | Plan 06-01 Task 1 audits git history for committed secrets BEFORE any push happens. Task 1 of this plan instructs user to confirm `.env` not in push (`git ls-files | grep '\.env$'`). |
| T-06-09 | I | `VITE_NASA_API_KEY` inlined in production bundle | accept | Documented in DEPLOYMENT.md Prerequisites and CLAUDE.md Constraints. NASA keys are low-sensitivity (free tier, rate-limited, no PII access). If higher-sensitivity keys are added later, route through a Vercel Edge Function. |
| T-06-10 | S (Spoofing) | User's resume signal authenticity | accept | This is a solo-dev workflow — only the user can issue resume signals. No multi-actor coordination. If the user lies about `cors verified` (e.g.), the consequences hit them directly when sharing the URL fails. |
| T-06-11 | T (Tampering) | Env var name typo in Vercel dashboard | mitigate | Task 3 specifies the exact env var name (`VITE_NASA_API_KEY` — capital VITE_, exact underscores) and Task 5's verification step explicitly inspects DONKI request URLs to confirm the real key is in use (not DEMO_KEY). DEPLOYMENT.md Troubleshooting also documents the symptom. |
| T-06-12 | D | NASA DONKI rate-limit DoS in production | mitigate | D-05 mandates the real NASA API key (1000/hr) instead of DEMO_KEY (30/hr). Task 5 verifies via DONKI URL inspection that the real key is in use. |
| T-06-13 | I | Production deploy reveals app source via `view-source:` | accept | This is a static SPA; the entire JS bundle is necessarily readable by the client. No proprietary logic; all data lives in upstream APIs. Acceptable for educational/portfolio scope. |
| T-06-14 | E (Elevation of privilege) | Vercel CLI state mutations by Claude | mitigate | Plan explicitly forbids Claude from running `vercel link`, `vercel deploy`, `vercel --prod`, `vercel env add/rm` — all state-modifying Vercel CLI commands are user-only per the planner's guardrails. Read-only commands (`vercel ls`, `vercel inspect`) would be acceptable but are not used in this plan. |
| T-06-15 | I | Production URL committed before user confirms it's correct | mitigate | Task 7 explicitly gates on Task 4 resume signal (`deployed <URL>`) AND Task 6 resume signal (`smoke passed`). If either is missing, Task 7 stops. The placeholder `(coming soon)` survives until both confirmations are in. |

</threat_model>

<verification>

**Plan-level checks (run after Task 7 completes — all earlier tasks are user-action checkpoints):**

```bash
set -e

# Production URL is set (not placeholder) in README
grep -qE '^\*\*Live demo:\*\* https://[a-z0-9-]+\.vercel\.app' README.md
! grep -q '\*\*Live demo:\*\* (coming soon' README.md

# PROJECT.md decision validated
grep -q 'Vercel deployment.*✓ Good (2026-05-21' .planning/PROJECT.md
grep -qE '^- \[x\] Vercel deployment with .* validated 2026-05-21' .planning/PROJECT.md

# REQUIREMENTS.md DEPLOY-* all flipped
[ "$(grep -cE '^- \[x\] \*\*DEPLOY-0[1-4]' .planning/REQUIREMENTS.md | tr -d ' ')" = "4" ]
[ "$(grep -cE '^\| DEPLOY-0[1-4] \| Phase 6: Vercel Deployment \| Complete' .planning/REQUIREMENTS.md | tr -d ' ')" = "4" ]
! grep -qE '^\| DEPLOY-0[1-4] \| Phase 6: Vercel Deployment \| Pending' .planning/REQUIREMENTS.md

# No source code regressions
git diff --name-only src/ 2>/dev/null | grep -q . && echo "FAIL: src/ modified" && exit 1 || true

# Build still green
npm run build > /tmp/06-02-verify-build.log 2>&1
grep -qE '✓ built in' /tmp/06-02-verify-build.log

# Vercel CLI state-mutating commands NOT in plan SUMMARY (defensive — confirm planner discipline)
! grep -E 'vercel (link|deploy|--prod|env add|env rm)' .planning/phases/06-vercel-deployment/06-02-SUMMARY.md 2>/dev/null | grep -v 'user actions\|user runs\|user-action\|Path B' | grep -q .
```

**ROADMAP Phase 6 success criteria observable:**
1. `git push origin main` triggers Vercel auto-deploy → verified during Task 4
2. Production URL serves Mars + Moon tabs with live MAAS2/SWPC/DONKI data, CORS-clean → verified Tasks 5 + 6
3. DONKI uses `VITE_NASA_API_KEY` (not DEMO_KEY) → verified Task 5 step 6
4. Production preview matches local end-to-end → verified Task 6

</verification>

<success_criteria>

Plan 06-02 succeeds when:

- [ ] User pushed repo to GitHub (Task 1 resume: `pushed <username>`)
- [ ] User imported the project in Vercel (Task 2 resume: `linked A` or `linked B`)
- [ ] User set `VITE_NASA_API_KEY` env var with Production + Preview scopes (Task 3 resume: `env set`)
- [ ] User triggered first deploy and provided the production URL (Task 4 resume: `deployed <URL>`)
- [ ] User verified 7 production fetches all return 200 with zero CORS errors AND DONKI uses real key (Task 5 resume: `cors verified`)
- [ ] User smoke-tested both tabs end-to-end (Task 6 resume: `smoke passed`)
- [ ] Claude updated README.md, PROJECT.md, REQUIREMENTS.md autonomously after gates cleared (Task 7)
- [ ] DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04 all marked Complete in REQUIREMENTS.md
- [ ] `npm run build` still exits 0 (no doc-edit regression)
- [ ] Claude never ran `git push`, `vercel link`, `vercel deploy`, `vercel --prod`, `vercel env add`, or any other state-modifying CLI/API call against shared infrastructure

</success_criteria>

<output>

After completion, create `.planning/phases/06-vercel-deployment/06-02-SUMMARY.md` capturing:

- The GitHub repo URL the user pushed to (from Task 1 resume signal)
- Which Vercel linkage path was used (A or B, from Task 2 resume signal)
- Confirmation that `VITE_NASA_API_KEY` was set with Production + Preview scopes (from Task 3 resume signal)
- The exact production URL the user captured (from Task 4 resume signal)
- The 7 verified production fetches list with status codes (from Task 5 verification)
- Smoke-test pass note (from Task 6 verification)
- Commit SHA(s) for the README.md / PROJECT.md / REQUIREMENTS.md updates from Task 7
- Final status of DEPLOY-01..DEPLOY-04 (all Complete)
- A "Phase 6 complete; v1.0 milestone shipped" closing note

</output>
