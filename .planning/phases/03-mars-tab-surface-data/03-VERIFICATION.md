---
phase: 03-mars-tab-surface-data
verified: 2026-05-19T00:00:00Z
status: human_needed
score: 5/5 ROADMAP success criteria verified (automated); 3 visual/UAT items require human confirmation
overrides_applied: 1
overrides:
  - must_have: "User sees 'From Curiosity Rover / REMS instrument' source attribution"
    reason: "Locked in 03-CONTEXT.md D-12 as 'From Curiosity Rover · REMS instrument' (middle-dot U+00B7 instead of slash) — a deliberate divergence from the ROADMAP literal. The implemented form preserves the spirit of MARS-10 and is the canonical D-12 string referenced across plan 03-02 acceptance criteria and the Phase 3 ROADMAP note."
    accepted_by: "Will McDermott (planner) via 03-CONTEXT.md D-12 lock"
    accepted_at: "2026-05-19T00:00:00Z"
re_verification: null
human_verification:
  - test: "Open `npm run dev`, switch to the Mars tab, and observe initial render against the live MAAS2 endpoint"
    expected: "Eight DataCards populate with real Curiosity REMS values for the latest sol: Sol (integer), Earth Date (YYYY-MM-DD string), Min Temp / Max Temp (°C, one decimal), Pressure (Pa, integer), Humidity (%, one decimal), Wind Speed (m/s, one decimal), Atmospheric Opacity (categorical string like 'Sunny'). Section subtitles ('Sol Context', 'Temperature & Atmosphere', 'Wind & Sky') and the italic source line 'From Curiosity Rover · REMS instrument' are visible. Tab-level LastUpdated chip in the header reads 'Last updated: just now' (or 'X mins ago' on revisit). One per-card LastUpdated chip sits below every DataCard."
    why_human: "Live HTTP fetch to MAAS2, JSON parsing, React Query mount cycle, and visual layout require a running browser. Static greps verify the wiring exists but not that MAAS2 actually returns data or that the cards render correctly."
  - test: "Hover any Mars data value (e.g., Pressure) and verify the tooltip surfaces the Earth-anchored copy"
    expected: "After ~150ms hover delay, a portal tooltip appears containing the verbatim Discussion.md §5 copy for that key (e.g., for Pressure: 'Mars' atmosphere is about 0.6% as dense as Earth's — too thin to breathe, thick enough to drive dust storms.'). Tooltip footer shows 'MAAS2 / Curiosity REMS' source attribution. All eight mars.* keys should resolve to distinct copy when hovered."
    why_human: "TooltipWrapper portal rendering, 150ms timing, and Earth-anchored copy resolution per key cannot be confirmed without browser interaction. Phase 2 verified the TooltipWrapper primitive; Phase 3 wired the keys."
  - test: "Simulate MAAS2 failure (e.g., DevTools → Network → block api.maas2.apollorion.com) and observe the error state"
    expected: "All eight DataCards render the muted italic string 'Data temporarily unavailable'. The tab heading 'Mars — Surface Conditions', source attribution line 'From Curiosity Rover · REMS instrument', and all three section subtitles ('Sol Context', 'Temperature & Atmosphere', 'Wind & Sky') remain visible — the tab is never blank. The tab-level + per-card LastUpdated chips render em-dash ('—') because dataUpdatedAt is 0 before the first successful fetch. After ~1 hour (or unblocking the network), background refetch silently restores values with no skeleton flash."
    why_human: "Error simulation requires DevTools network manipulation; visual confirmation of 'never blank' requirement and silent background refetch (success criterion 5) needs human observation of the rendered DOM in different states."
---

# Phase 3: Mars Tab — Surface Data Verification Report

**Phase Goal:** A user can open the Mars tab and see live Curiosity REMS conditions for the latest sol with units, tooltips, source attribution, and per-card freshness.

**Verified:** 2026-05-19
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (merged from ROADMAP success criteria + PLAN 03-02 must_haves)

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | User opening the Mars tab sees real MAAS2 data for the latest sol — sol number, Earth date, min/max temperature, pressure, wind speed, humidity, atmospheric opacity — each with its unit (ROADMAP SC-1)                                                                                                                                                                                                                                     | ? UNCERTAIN (wiring VERIFIED)           | `MarsTab.jsx` calls `useMarsData()`, binds eight MAAS2 fields (`sol`, `terrestrial_date`, `min_temp`, `max_temp`, `pressure`, `wind_speed`, `humidity`, `atmo_opacity`) to eight DataCards with units `°C` (min/max temp), `Pa` (pressure), `m/s` (wind speed), `%` (humidity) — no unit for sol/date/opacity per D-25. `formatInt` for sol/pressure (Math.round); `formatOneDecimal` for temp/wind/humidity (toFixed(1)); raw passthrough for terrestrial_date / atmo_opacity per D-27/D-15. `App.jsx` mounts `<MarsTab />` on `activeTab === TABS.MARS.id`. Live MAAS2 fetch confirmation is human-only.                                                                                                                                                            |
| 2   | User hovering any Mars value sees an Earth-anchored plain-language tooltip (ROADMAP SC-2)                                                                                                                                                                                                                                                                                                                                                    | ? UNCERTAIN (wiring VERIFIED)           | `tooltips.js` exports all eight `mars.*` keys (sol/earthDate/minTemp/maxTemp/pressure/windSpeed/humidity/opacity) with verbatim Discussion.md §5 copy. Earth comparisons present: `0.6% as dense as Earth's` (pressure), `Gale Crater. Mars nights can plunge to -80°C` (minTemp), `slightly longer than an Earth day` (sol), etc. Each entry includes `source: 'MAAS2 / Curiosity REMS'` (grep count = 8). `MarsTab.jsx` passes the correct `tooltipKey="mars.*"` to every DataCard (all 8 keys grep-verified). DataCard composes TooltipWrapper via `getTooltip(tooltipKey)`. Hover behavior is human-only.                                                                                                                                                       |
| 3   | User sees "From Curiosity Rover · REMS instrument" source attribution (ROADMAP SC-3, overridden per D-12)                                                                                                                                                                                                                                                                                                                                    | ✓ VERIFIED (override)                  | `MarsTab.jsx` line 23: `const SOURCE_LINE = 'From Curiosity Rover · REMS instrument'`; rendered on line 77 as `<p className="text-mars-50/60 text-sm italic">{SOURCE_LINE}</p>` inside the `<header>` directly under the `<h2>Mars — Surface Conditions</h2>`. Color `text-mars-50/60` per D-12. ROADMAP literal uses `/` but 03-CONTEXT D-12 explicitly locks the middle-dot `·` (U+00B7) form — recorded as override above.                                                                                                                                                                                                                                                                                                                                       |
| 4   | User sees per-card last-updated timestamps and a graceful "Data temporarily unavailable" message if MAAS2 fails — the tab never goes fully blank (ROADMAP SC-4)                                                                                                                                                                                                                                                                              | ? UNCERTAIN (wiring VERIFIED)           | `MarsTab.jsx` mounts 9 `<LastUpdated>` chips (1 in header + 8 per-card, one beneath every DataCard via the `<div className="flex flex-col gap-2">` wrapper pattern), all sharing the same `new Date(dataUpdatedAt)` timestamp with `palette="mars"`. `cardState = isLoading ? 'loading' : isError ? 'error' : 'ok'` propagates a shared state to all 8 DataCards. DataCard (Phase 2 contract) renders the exact string "Data temporarily unavailable" when `state === 'error'`. Header (`<h2>`, source line) and three `<section>` subtitles (`Sol Context`, `Temperature & Atmosphere`, `Wind & Sky`) sit OUTSIDE the DataCard render branch, so they remain visible across all states. Visual "never blank" confirmation is human-only.                            |
| 5   | User's Mars tab refreshes silently in the background on a 1-hour cadence without blanking the UI (ROADMAP SC-5)                                                                                                                                                                                                                                                                                                                              | ✓ VERIFIED                              | `useMarsData.js` line 47: `refetchInterval: ONE_HOUR_MS` where `ONE_HOUR_MS = 1000 * 60 * 60`. Line 48: `refetchOnWindowFocus: false`. `MarsTab.jsx` line 47: `cardState = isLoading ? 'loading' : isError ? 'error' : 'ok'` — `isFetching` is NEVER read (grep returns 0). Practical effect: TanStack Query keeps `isLoading=false` during the hourly tick, so prior values stay on screen with no skeleton flash. The D-18 / SC-5 silent-refetch lock holds at the wiring level. (Time-based UI test is impractical in <10s spot-check window; lock is verified at code level.)                                                                                                                                                                                    |

**Score:** 2/5 fully VERIFIED programmatically; 3/5 structurally VERIFIED but require human visual confirmation (truths 1, 2, 4). All 5 ROADMAP success criteria have correct wiring evidence in the codebase.

### Required Artifacts

| Artifact                          | Expected                                                                                                                                                                       | Status     | Details                                                                                                                                                                                                                                                                                                                       |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/hooks/useMarsData.js`        | TanStack Query hook fetching MAAS2 latest sol on 1h cadence; manual `text → JSON.parse` for Content-Type quirk; `refetchOnWindowFocus: false`; named export `useMarsData`     | ✓ VERIFIED | 50 lines (≥25 min); named export `useMarsData` confirmed via runtime ESM import (`typeof m.useMarsData === 'function'`). Imports `useQuery` from `@tanstack/react-query`. URL constant `'https://api.maas2.apollorion.com/'`. `ONE_HOUR_MS = 1000 * 60 * 60`. `refetchOnWindowFocus: false` literal present. Uses `r.text()` + `JSON.parse(text)`; `r.json()` and `response.json()` absent (grep negative). Throws on `!response.ok` and on JSON parse failure. No `VITE_NASA_API_KEY`, no `console.log`. |
| `src/constants/tooltips.js`       | Eight `mars.*` tooltip keys with verbatim Discussion.md §5 copy + `source: 'MAAS2 / Curiosity REMS'`; `mars.example` placeholder removed; getTooltip accessor untouched         | ✓ VERIFIED | 90 lines; all 8 `mars.*` keys (`sol`, `earthDate`, `minTemp`, `maxTemp`, `pressure`, `windSpeed`, `humidity`, `opacity`) present and resolve via runtime ESM import; `mars.example` absent (`m.TOOLTIPS['mars.example']` returns `false`). `MAAS2 / Curiosity REMS` source string appears exactly 8 times. Earth-anchored substrings present: `0.6% as dense as Earth`, `Gale Crater`. `export function getTooltip` preserved. Phase 4 placeholders `moon.example`, `swpc.radiationRisk`, `donki.cme` left intact (correct — Phase 3 must not touch Phase 4 entries). |
| `src/tabs/MarsTab.jsx`            | Live Mars tab — 8 DataCards bound to useMarsData() fields, 9 LastUpdated chips, source attribution, shared cardState, palette="mars" everywhere, tabpanel wrapper preserved   | ✓ VERIFIED | 197 lines (≥100 min); imports `DataCard`, `LastUpdated`, `useMarsData`. Exactly 8 `<DataCard` (grep -c = 8) and 9 `<LastUpdated` (grep -c = 9). 17 `palette="mars"` occurrences (8 DataCard + 9 LastUpdated = 17 minimum, matches). All 8 `tooltipKey="mars.*"` props present. `cardState` reads `isLoading`, not `isFetching` (`isFetching` grep returns 0). Source string `'From Curiosity Rover · REMS instrument'` literal present. `<section role="tabpanel" aria-label="Mars conditions">` wrapper preserved. No `StatusBadge` / `AlertCard` / `TS_CME_DEMO` / `TS_JUST_NOW` / `Primitive Demo Gallery` residue. Three section subtitles present (Sol Context / Temperature & Atmosphere / Wind & Sky). `text-mars-accent/70` (D-09) and `text-mars-50/60` (D-12) classes present. Inline `Math.round` (formatInt) and `toFixed(1)` (formatOneDecimal) helpers present. Raw passthrough for `terrestrial_date` and `atmo_opacity`. |

### Key Link Verification

| From                                  | To                                          | Via                                                                                       | Status     | Details                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/hooks/useMarsData.js`            | `https://api.maas2.apollorion.com/`         | `fetch(MAAS2_URL)` + `r.text()` + `JSON.parse(text)`                                       | ✓ WIRED    | Line 25 declares URL constant; line 31 `fetch(MAAS2_URL)`; lines 35-37 read body as text then `JSON.parse`. Throws if `!response.ok` or parse fails — TanStack Query will set `isError: true`.                                                                                                                                                |
| `src/hooks/useMarsData.js`            | `@tanstack/react-query`                     | `import { useQuery } from '@tanstack/react-query'`                                          | ✓ WIRED    | Line 1 import; line 44 calls `useQuery({...})` with `queryKey: ['mars','maas2','latest']`, `queryFn: fetchMarsData`, `refetchInterval: ONE_HOUR_MS`, `refetchOnWindowFocus: false`. Returns the full query result for consumer destructuring.                                                                                                  |
| `src/tabs/MarsTab.jsx`                | `src/hooks/useMarsData.js`                  | `import { useMarsData } from '../hooks/useMarsData.js'` + `useMarsData()` call            | ✓ WIRED    | Line 3 import; line 42 `const { data, isLoading, isError, dataUpdatedAt } = useMarsData()`. Eight field bindings derived from `data` (sol, earthDate, minTemp, maxTemp, pressure, windSpeed, humidity, opacity) feed eight DataCards.                                                                                                          |
| `src/tabs/MarsTab.jsx`                | `src/components/DataCard.jsx`               | `import DataCard from '../components/DataCard.jsx'` + 8 `<DataCard>` JSX nodes             | ✓ WIRED    | Line 1 import; eight DataCard elements with `value`, `unit` (where applicable), `tooltipKey`, `state={cardState}`, `palette="mars"`. Each one receives a formatter-produced value derived from `data`.                                                                                                                                       |
| `src/tabs/MarsTab.jsx`                | `src/components/LastUpdated.jsx`            | `import LastUpdated from '../components/LastUpdated.jsx'` + 9 `<LastUpdated>` JSX nodes    | ✓ WIRED    | Line 2 import; one tab-level LastUpdated in `<header>` + eight per-card LastUpdated below each DataCard. All share the same `timestamp` variable: `dataUpdatedAt ? new Date(dataUpdatedAt) : null`. Pre-fetch null-guard prevents "N years ago" relative to epoch 0.                                                                          |
| `src/tabs/MarsTab.jsx` (tooltipKey)   | `src/constants/tooltips.js` (mars.* keys)   | `DataCard` internally calls `getTooltip(tooltipKey)` → `TooltipWrapper` (Phase 2 contract) | ✓ WIRED    | All 8 `tooltipKey="mars.{sol,earthDate,minTemp,maxTemp,pressure,windSpeed,humidity,opacity}"` props match the 8 keys exported from tooltips.js. Runtime ESM smoke test confirms each key resolves to a `{text, source}` object.                                                                                                                 |
| `src/App.jsx`                         | `src/tabs/MarsTab.jsx`                      | `import MarsTab from './tabs/MarsTab.jsx'` + conditional `{activeTab === TABS.MARS.id && <MarsTab />}` | ✓ WIRED    | Phase 1 contract preserved; opening the Mars tab in the running app actually mounts the Phase 3 component.                                                                                                                                                                                                                                  |

### Data-Flow Trace (Level 4)

The Mars tab renders dynamic data from a single useQuery hook. Tracing the data variable through to render confirms real data flows end-to-end at the wiring level.

| Artifact                        | Data Variable                                          | Source                                                                                  | Produces Real Data                                  | Status        |
| ------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------- | --------------------------------------------------- | ------------- |
| `useMarsData.js`                | `response.text() → JSON.parse(text)`                   | Live HTTP GET to `https://api.maas2.apollorion.com/`                                    | Yes (when MAAS2 endpoint is reachable)              | ✓ FLOWING     |
| `MarsTab.jsx` — `data` field    | `useMarsData().data` (TanStack Query)                  | The hook's queryFn return value (the parsed MAAS2 payload)                              | Yes — payload contains all 8 expected REMS fields    | ✓ FLOWING     |
| `MarsTab.jsx` — `dataUpdatedAt` | `useMarsData().dataUpdatedAt` (TanStack Query)         | TanStack Query writes epoch ms on every successful queryFn resolution                   | Yes — populated after first successful fetch         | ✓ FLOWING     |
| 8 DataCard `value` props        | Formatted derivatives of `data.{sol,terrestrial_date,...}` | Inline `formatInt(data.sol)`, `formatOneDecimal(data.min_temp)`, raw `data.terrestrial_date`, raw `data.atmo_opacity` | Yes — every formatter reads from the live `data` object | ✓ FLOWING     |
| 9 LastUpdated `timestamp` prop  | `dataUpdatedAt ? new Date(dataUpdatedAt) : null`       | The TanStack Query timestamp; null before first fetch                                   | Yes — Date after first fetch, null otherwise (renders em-dash) | ✓ FLOWING     |
| 8 DataCard `state` prop         | Shared `cardState` from `isLoading` + `isError`        | Derived from useMarsData query state                                                    | Yes — drives `'loading' \| 'error' \| 'ok'` per Phase 2 DataCard contract | ✓ FLOWING     |

No HOLLOW or DISCONNECTED findings. The single useMarsData call fans out to all 8 cards and 9 LastUpdated chips — Discussion.md §5's "one fetch → one timestamp" honesty contract holds. Static fallbacks are intentional (formatter returns `null` → DataCard renders em-dash when MAAS2 omits a field), not stubs.

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                          | Result                                                                | Status   |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------- | -------- |
| Production build succeeds                                            | `npm run build`                                                                  | Exit 0; 92 modules; 1.54s; 199.21 kB / 62.81 kB gzipped                | ✓ PASS   |
| `useMarsData` exports as a function (ESM import smoke test)          | `node --input-type=module -e "import('./src/hooks/useMarsData.js')...`           | `typeof m.useMarsData === 'function'`; only export is `useMarsData`    | ✓ PASS   |
| All 8 mars.* tooltip keys resolve at runtime                          | `node --input-type=module -e "import('./src/constants/tooltips.js')..."`         | `Missing: []`; `mars.pressure.text` starts with `Mars' atmosphere is about 0.6% as dense`; source = `MAAS2 / Curiosity REMS` | ✓ PASS   |
| `mars.example` placeholder removed                                    | Runtime ESM import: `!!m.TOOLTIPS['mars.example']`                                | `false`                                                                | ✓ PASS   |
| Exactly 8 DataCards in MarsTab.jsx                                    | `grep -c '<DataCard' src/tabs/MarsTab.jsx`                                       | `8`                                                                    | ✓ PASS   |
| Exactly 9 LastUpdated chips in MarsTab.jsx                            | `grep -c '<LastUpdated' src/tabs/MarsTab.jsx`                                    | `9`                                                                    | ✓ PASS   |
| At least 17 `palette="mars"` occurrences (8 cards + 9 chips)         | `grep -c 'palette="mars"' src/tabs/MarsTab.jsx`                                  | `17`                                                                   | ✓ PASS   |
| All 8 `tooltipKey="mars.*"` props present                             | `grep -q 'tooltipKey="mars.X"'` for each of 8 keys                                | All 8 found                                                            | ✓ PASS   |
| `isFetching` never read (D-18 silent-refetch lock)                    | `grep -q "isFetching" src/tabs/MarsTab.jsx`                                      | Not found                                                              | ✓ PASS   |
| `r.json()` / `response.json()` not used (D-04 Content-Type lock)     | `grep -q "r\.json()" src/hooks/useMarsData.js` + `grep -q "response.json()" ...` | Neither found                                                          | ✓ PASS   |
| 1-hour interval literal present                                       | `grep -q "1000 \* 60 \* 60" src/hooks/useMarsData.js`                            | Found                                                                  | ✓ PASS   |
| `refetchOnWindowFocus: false` literal present                         | `grep -q "refetchOnWindowFocus: false" src/hooks/useMarsData.js`                  | Found                                                                  | ✓ PASS   |
| Source line `'From Curiosity Rover · REMS instrument'` literal       | `grep -q "From Curiosity Rover · REMS instrument" src/tabs/MarsTab.jsx`           | Found                                                                  | ✓ PASS   |
| No StatusBadge / AlertCard residue on Mars tab                        | `! grep -q "StatusBadge\\|AlertCard" src/tabs/MarsTab.jsx`                       | Both absent                                                            | ✓ PASS   |
| No Phase 2 demo-gallery mock constants                                | `! grep -q "TS_CME_DEMO\\|TS_JUST_NOW\\|Primitive Demo Gallery" src/tabs/MarsTab.jsx` | All three absent                                                  | ✓ PASS   |
| `<section role="tabpanel" aria-label="Mars conditions">` preserved   | `grep -q 'role="tabpanel"' src/tabs/MarsTab.jsx` + `grep -q 'aria-label="Mars conditions"' ...` | Both found                                            | ✓ PASS   |
| App.jsx mounts MarsTab on Mars-tab selection                          | `grep -E "MarsTab" src/App.jsx`                                                   | `import MarsTab from './tabs/MarsTab.jsx'` + `{activeTab === TABS.MARS.id && <MarsTab />}` | ✓ PASS   |
| Live MAAS2 endpoint actually returns expected payload                 | (would require a real HTTP fetch + JSON shape check)                              | Skipped — falls under human UAT (see human_verification item 1)        | ? SKIP   |

### Requirements Coverage

All 11 MARS-XX requirement IDs from PLAN frontmatter are accounted for and traceable to commits + code.

| Requirement | Source Plan | Description                                                                                                                | Status      | Evidence                                                                                                                                                                                                                                                                                                                |
| ----------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MARS-01     | 03-01       | `useMarsData` React Query hook fetches latest sol from MAAS2 with `refetchInterval: 1h`                                     | ✓ SATISFIED | `src/hooks/useMarsData.js` (commit `6a0fc5d`): named export, `useQuery({queryKey: ['mars','maas2','latest'], queryFn, refetchInterval: ONE_HOUR_MS (1000*60*60), refetchOnWindowFocus: false})`. Content-Type quirk handled via `r.text() → JSON.parse(text)`.                                                          |
| MARS-02     | 03-02       | Display Sol number (Martian day) with tooltip explaining sol length (~24h 37min)                                            | ✓ SATISFIED | `MarsTab.jsx` line 89-95: `<DataCard label="Sol" value={sol} tooltipKey="mars.sol" state={cardState} palette="mars" />`. Tooltip copy in `tooltips.js`: `'A Martian day (sol) is about 24 hours and 37 minutes — slightly longer than an Earth day.'`. `sol` formatted via `formatInt`.                                  |
| MARS-03     | 03-02       | Display Earth date (`terrestrial_date`) corresponding to the sol                                                            | ✓ SATISFIED | `MarsTab.jsx` line 100-105: `<DataCard label="Earth Date" value={earthDate} tooltipKey="mars.earthDate" />`. `earthDate = data.terrestrial_date` (raw YYYY-MM-DD passthrough per D-27).                                                                                                                                  |
| MARS-04     | 03-02       | Display Min Temperature in °C with tooltip anchoring against Mars overnight lows                                            | ✓ SATISFIED | `MarsTab.jsx` line 119-125: `<DataCard label="Min Temp" value={minTemp} unit="°C" tooltipKey="mars.minTemp" />`. Tooltip: `'Overnight lows at Gale Crater. Mars nights can plunge to -80°C or colder.'`. `minTemp` formatted via `formatOneDecimal` (toFixed(1)).                                                        |
| MARS-05     | 03-02       | Display Max Temperature in °C with tooltip anchoring against daytime highs                                                  | ✓ SATISFIED | `MarsTab.jsx` line 129-135: `<DataCard label="Max Temp" value={maxTemp} unit="°C" tooltipKey="mars.maxTemp" />`. Tooltip: `'Daytime highs. Even the warmest Mars days rarely exceed 0°C at the surface.'`                                                                                                                |
| MARS-06     | 03-02       | Display Atmospheric Pressure in Pa with tooltip comparing to Earth (~0.6%)                                                 | ✓ SATISFIED | `MarsTab.jsx` line 140-146: `<DataCard label="Pressure" value={pressure} unit="Pa" tooltipKey="mars.pressure" />`. Tooltip: `"Mars' atmosphere is about 0.6% as dense as Earth's — too thin to breathe, thick enough to drive dust storms."`. `pressure` formatted via `formatInt` (integer Pa per D-26).                |
| MARS-07     | 03-02       | Display Wind Speed in m/s with tooltip explaining why high speeds feel gentle on Mars                                       | ✓ SATISFIED | `MarsTab.jsx` line 171-177: `<DataCard label="Wind Speed" value={windSpeed} unit="m/s" tooltipKey="mars.windSpeed" />`. Tooltip: `'Average near-surface wind. Mars winds feel gentle despite high speeds due to the thin atmosphere.'`                                                                                   |
| MARS-08     | 03-02       | Display Humidity (%) with tooltip explaining dryness and water-ice context                                                  | ✓ SATISFIED | `MarsTab.jsx` line 151-157: `<DataCard label="Humidity" value={humidity} unit="%" tooltipKey="mars.humidity" />`. Tooltip: `'Relative humidity. Mars air is extremely dry — water ice exists but liquid water cannot persist on the surface.'`                                                                            |
| MARS-09     | 03-02       | Display Atmospheric Opacity (categorical, e.g., "Sunny" / "Dusty") with tooltip on dust storms                              | ✓ SATISFIED | `MarsTab.jsx` line 182-188: `<DataCard label="Atmospheric Opacity" value={opacity} tooltipKey="mars.opacity" />`. No unit (D-25). Raw passthrough of `data.atmo_opacity` (D-15). Tooltip: `"Describes dust loading in the atmosphere. 'Sunny' means clear skies; 'Dusty' can signal regional or global storms."`           |
| MARS-10     | 03-02       | Source attribution: "From Curiosity Rover / REMS instrument" visible on the tab                                              | ✓ SATISFIED (override) | `MarsTab.jsx` line 23: `const SOURCE_LINE = 'From Curiosity Rover · REMS instrument'`. Per 03-CONTEXT D-12 lock, the literal `/` is replaced with the middle-dot `·` (U+00B7) — recorded as an override above. The spirit of MARS-10 (visible Curiosity / REMS source attribution) is fully met.                              |
| MARS-11     | 03-02       | Every data card shows per-card last-updated timestamp and a graceful "Data temporarily unavailable" error state             | ✓ SATISFIED | `MarsTab.jsx` mounts 8 per-card LastUpdated chips (one below every DataCard via the `<div className="flex flex-col gap-2">` wrapper pattern). DataCard (Phase 2 contract) renders the exact string "Data temporarily unavailable" when `state === 'error'` — `cardState` flips to `'error'` on `useMarsData().isError`.    |

No ORPHANED requirements: REQUIREMENTS.md maps exactly MARS-01..MARS-11 to Phase 3, and all 11 IDs appear across the two PLAN frontmatter `requirements:` blocks (03-01 covers MARS-01; 03-02 covers MARS-02..MARS-11).

### Anti-Patterns Found

| File                          | Line | Pattern                                                                  | Severity | Impact                                                                                                                            |
| ----------------------------- | ---- | ------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `src/constants/tooltips.js`   | 18   | The word `placeholders` appears in JSDoc (`Phase 2 seeded placeholders...`) | ℹ️ Info  | This is documentation describing the historical Phase 2 seeding contract, not actual placeholder code or values. Not a stub. |

No TODO / FIXME / XXX / HACK markers anywhere in Phase 3 sources. No `console.log` debug noise. No empty arrays / objects rendered as data (the only `=> null` returns are intentional formatter null-on-NaN guards that DataCard interprets as em-dash per the Phase 2 contract).

### Human Verification Required

See the YAML frontmatter `human_verification` block. Three items require browser-based confirmation:

1. **Live MAAS2 fetch & populated cards** — Eight DataCards populate with real Curiosity REMS values for the latest sol; section subtitles and source attribution visible; per-card LastUpdated chips render relative time.
2. **Tooltip hover surfacing** — Hover any value to see the verbatim Discussion.md §5 copy in a portal tooltip with `MAAS2 / Curiosity REMS` source attribution.
3. **Error state + silent background refetch** — Block MAAS2 in DevTools and confirm all 8 cards render "Data temporarily unavailable" while heading + source + section subtitles remain visible (tab never blank). Confirm hourly background refetch is silent (no skeleton flash on the 1h tick).

These mirror the established Phase 1 / Phase 2 visual-UAT division: static greps verify the wiring; the browser verifies the rendered behavior.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria have correct wiring evidence in the codebase; all 3 required artifacts exist, are substantive, are properly imported and called, and produce real data (Level 4 data-flow trace passes end-to-end). All 11 MARS-XX requirement IDs are traceable to commits (`6645b92` for tooltips, `6a0fc5d` for the hook, `0c6a8ca` for the MarsTab rewrite) and to specific lines of source code. `npm run build` exits 0.

The ROADMAP-vs-D-12 source-string divergence (`/` → `·`) is an intentional, documented planner decision locked in 03-CONTEXT.md D-12 — recorded as an override and does NOT count as a gap against MARS-10.

Three behavioral items require human UAT (live MAAS2 fetch, tooltip hover, error/silent-refetch states) because they involve real HTTP, browser rendering, or DevTools network simulation that static analysis cannot perform.

---

_Verified: 2026-05-19_
_Verifier: Claude (gsd-verifier)_
